# ==============================================================================
# Layers Frontend Environment Configuration
# ==============================================================================
# This file is auto-generated by scripts/generate-env.sh when running:
#   pnpm dev        # Local mode (127.0.0.1)
#   pnpm dev:tunnel # Tunnel mode (ngrok/localtunnel URL)
#
# You can also copy this file to .env.local for manual configuration:
#   cp .env.example .env.local
# ==============================================================================

# OAuth configuration
# ATProto requires 127.0.0.1 (NOT localhost) for loopback OAuth
NEXT_PUBLIC_OAUTH_BASE_URL=http://127.0.0.1:3000

# Backend API URL
NEXT_PUBLIC_API_URL=http://127.0.0.1:3001

# Development mode
# - "local": Uses 127.0.0.1 loopback OAuth (default)
# - "tunnel": Uses tunnel URL for OAuth, adds Bypass-Tunnel-Reminder header
NEXT_PUBLIC_DEV_MODE=local

# ==============================================================================
# Observability (Grafana Faro)
# ==============================================================================
# Faro collector URL for sending telemetry data
# Required in production, optional in development
#
# Self-hosted (via docker-compose.observability.yml):
#   NEXT_PUBLIC_FARO_URL=http://localhost:12347/collect
#
# Production self-hosted:
#   NEXT_PUBLIC_FARO_URL=https://faro.layers.pub/collect
#
# Grafana Cloud:
#   NEXT_PUBLIC_FARO_URL=https://faro-collector-prod-us-east-0.grafana.net/collect/<app-id>
#
# NEXT_PUBLIC_FARO_URL=

# Application version for telemetry attribution
# In CI, set to: NEXT_PUBLIC_APP_VERSION=${{ github.sha }}
NEXT_PUBLIC_APP_VERSION=0.1.0

# Trace sampling rate (0.0 - 1.0)
# Controls what percentage of traces are captured
# Development: 1.0 (100%), Production: 0.1 (10%)
NEXT_PUBLIC_FARO_TRACE_SAMPLE_RATE=1.0

# Session sampling rate (0.0 - 1.0)
# Controls what percentage of user sessions are tracked
# Development: 1.0 (100%), Production: 0.5 (50%)
NEXT_PUBLIC_FARO_SESSION_SAMPLE_RATE=1.0

# Disable Faro entirely (set to "false" to disable)
# NEXT_PUBLIC_FARO_ENABLED=true
