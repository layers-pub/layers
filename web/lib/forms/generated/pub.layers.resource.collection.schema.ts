// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.resource.collection` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.resource.collection` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  createdAt: z.string().datetime({ offset: true }),
  description: z.string().max(10000).optional(),
  features: z.unknown().optional(),
  kind: z.union([z.enum(['lexicon', 'frame-inventory', 'gazetteer', 'paradigm', 'stop-list', 'stimulus-pool', 'custom']), z.string()]).optional(),
  kindUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  knowledgeRefs: z.array(z.unknown()).max(32).optional(),
  language: z.string().max(32).optional(),
  metadata: z.unknown().optional(),
  name: z.string().max(512),
  ontologyRef: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  version: z.string().max(64).optional(),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
