// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.alignment.alignment` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.alignment.alignment` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  createdAt: z.string().datetime({ offset: true }),
  expression: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  features: z.unknown().optional(),
  kind: z.union([z.enum(['tokenization-to-tokenization', 'interlinear', 'parallel-text', 'audio-to-text', 'layer-to-layer', 'error-to-correction', 'custom']), z.string()]),
  kindUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  knowledgeRefs: z.array(z.unknown()).max(16).optional(),
  links: z.array(z.unknown()),
  metadata: z.unknown().optional(),
  source: z.unknown().optional(),
  sourceLang: z.string().max(64).optional(),
  subkind: z.union([z.enum(['word-to-morpheme', 'word-to-word', 'sentence-to-sentence', 'phrase-to-phrase', 'morpheme-to-gloss', 'forced-alignment', 'manual-alignment', 'custom']), z.string()]).optional(),
  subkindUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  target: z.unknown().optional(),
  targetLang: z.string().max(64).optional(),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
