// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.corpus.membership` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.corpus.membership` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  corpusRef: z.string().regex(/^at:\/\//, 'must start with at://'),
  createdAt: z.string().datetime({ offset: true }),
  expressionRef: z.string().regex(/^at:\/\//, 'must start with at://'),
  features: z.unknown().optional(),
  metadata: z.unknown().optional(),
  ordinal: z.number().int().min(0).optional(),
  split: z.union([z.enum(['train', 'dev', 'test', 'unlabeled']), z.string()]).optional(),
  splitUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
