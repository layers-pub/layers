// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.judgment.agreementReport` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.judgment.agreementReport` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  createdAt: z.string().datetime({ offset: true }),
  experimentRef: z.string().regex(/^at:\/\//, 'must start with at://'),
  features: z.unknown().optional(),
  judgmentSetRefs: z.array(z.string().regex(/^at:\/\//, 'must start with at://')).optional(),
  metric: z.union([z.enum(['cohens-kappa', 'fleiss-kappa', 'krippendorff-alpha', 'percent-agreement', 'correlation', 'f1', 'custom']), z.string()]).optional(),
  metricUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  numAnnotators: z.number().int().min(1).optional(),
  numItems: z.number().int().min(1).optional(),
  value: z.number().int().min(0).max(1000).optional(),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
