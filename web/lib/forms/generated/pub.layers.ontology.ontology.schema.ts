// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.ontology.ontology` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.ontology.ontology` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  createdAt: z.string().datetime({ offset: true }),
  description: z.string().max(10000).optional(),
  domain: z.union([z.enum(['general', 'biomedical', 'legal', 'financial', 'news', 'social-media', 'scientific', 'intelligence', 'dialogue', 'multimodal', 'custom']), z.string()]).optional(),
  domainUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  knowledgeRefs: z.array(z.unknown()).max(64).optional(),
  name: z.string().max(512),
  parentRef: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  personaRef: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  version: z.string().max(32).optional(),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
