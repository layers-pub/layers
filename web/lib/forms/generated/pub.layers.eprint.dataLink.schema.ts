// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.eprint.dataLink` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.eprint.dataLink` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  annotationRefs: z.array(z.string().regex(/^at:\/\//, 'must start with at://')).max(10000).optional(),
  corpusRef: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  createdAt: z.string().datetime({ offset: true }),
  dataKind: z.union([z.enum(['corpus', 'annotation-layer', 'model-output', 'gold-standard', 'evaluation-data', 'supplementary', 'replication']), z.string()]),
  dataKindUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  description: z.string().max(10000).optional(),
  eprintDid: z.string().optional(),
  eprintUri: z.string().regex(/^at:\/\//, 'must start with at://'),
  expressionRefs: z.array(z.string().regex(/^at:\/\//, 'must start with at://')).max(10000).optional(),
  features: z.unknown().optional(),
  paperSection: z.string().max(256).optional(),
  reproducibility: z.unknown().optional(),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
