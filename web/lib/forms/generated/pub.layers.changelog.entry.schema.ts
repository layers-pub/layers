// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.changelog.entry` record lexicon shape.

import { z } from 'zod';

export const schema = z.object({
  createdAt: z.string().datetime({ offset: true }),
  previousVersion: z.unknown().optional(),
  sections: z.array(z.unknown()).max(20),
  subject: z.string().regex(/^at:\/\//, 'must start with at://'),
  subjectCollection: z.string().max(256),
  summary: z.string().max(500),
  version: z.unknown().optional(),
});

export type SchemaInput = z.input<typeof schema>;
export type SchemaOutput = z.output<typeof schema>;
