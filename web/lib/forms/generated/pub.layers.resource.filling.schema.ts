// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.resource.filling` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.resource.filling` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  createdAt: z.string().datetime({ offset: true }),
  expressionRef: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  features: z.unknown().optional(),
  knowledgeRefs: z.array(z.unknown()).max(32).optional(),
  metadata: z.unknown().optional(),
  renderedText: z.string().max(100000).optional(),
  slotFillings: z.array(z.unknown()).max(64),
  strategy: z.union([z.enum(['exhaustive', 'random', 'stratified', 'mlm', 'csp', 'mixed', 'manual', 'custom']), z.string()]).optional(),
  strategyUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  templateRef: z.string().regex(/^at:\/\//, 'must start with at://'),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
