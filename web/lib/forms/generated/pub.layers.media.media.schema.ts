// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.media.media` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.media.media` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  audio: z.unknown().optional(),
  blob: z.unknown().optional(),
  createdAt: z.string().datetime({ offset: true }),
  description: z.string().max(10000).optional(),
  document: z.unknown().optional(),
  durationMs: z.number().int().min(0).optional(),
  externalUri: z.string().max(2048).url().optional(),
  features: z.unknown().optional(),
  fileSizeBytes: z.number().int().min(0).optional(),
  kind: z.union([z.enum(['audio', 'video', 'image', 'document']), z.string()]),
  kindUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  knowledgeRefs: z.array(z.unknown()).max(32).optional(),
  language: z.string().max(32).optional(),
  metadata: z.unknown().optional(),
  mimeType: z.string().max(128).optional(),
  parentMediaRef: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  startOffsetMs: z.number().int().min(0).optional(),
  title: z.string().max(1024).optional(),
  video: z.unknown().optional(),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
