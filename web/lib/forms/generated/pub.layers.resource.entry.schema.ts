// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.resource.entry` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.resource.entry` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  components: z.array(z.unknown()).max(32).optional(),
  createdAt: z.string().datetime({ offset: true }),
  features: z.unknown().optional(),
  form: z.string().max(4096),
  knowledgeRefs: z.array(z.unknown()).max(32).optional(),
  language: z.string().max(32).optional(),
  lemma: z.string().max(1024).optional(),
  metadata: z.unknown().optional(),
  mweKind: z.union([z.enum(['compound', 'phrasal-verb', 'idiom', 'light-verb', 'named-entity', 'collocation', 'custom']), z.string()]).optional(),
  mweKindUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  ontologyTypeRef: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  sourceRef: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
