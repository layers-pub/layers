// @generated by layers-codegen — do not edit.
// Regenerate with `cargo run -p layers-codegen -- frontend`.

// Zod schema mirroring the `pub.layers.ontology.typeDef` record lexicon shape.

import { z } from 'zod';

/** Validator for `pub.layers.ontology.typeDef` records — every constraint here is derived from the lexicon. */
export const schema = z.object({
  allowedRoles: z.array(z.unknown()).max(64).optional(),
  allowedValues: z.array(z.string().max(512)).max(256).optional(),
  createdAt: z.string().datetime({ offset: true }),
  features: z.unknown().optional(),
  gloss: z.string().max(10000).optional(),
  knowledgeRefs: z.array(z.unknown()).max(32).optional(),
  name: z.string().max(512),
  ontologyRef: z.string().regex(/^at:\/\//, 'must start with at://'),
  parentTypeRef: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
  typeKind: z.union([z.enum(['entity-type', 'situation-type', 'role-type', 'relation-type', 'attribute-type']), z.string()]),
  typeKindUri: z.string().regex(/^at:\/\//, 'must start with at://').optional(),
});

export type SchemaInput = z.input<typeof schema>;

export type SchemaOutput = z.output<typeof schema>;
