// @generated by idiolect-codegen. do not edit.
// source: pub.layers.persona.persona

//! Persona records define annotation frameworks and analyst perspectives. Different personas can annotate the same data with different ontologies and interpretive frameworks, following FOVEA's persona-based approach.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A persona representing an annotator's role, expertise, and interpretive framework.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Persona {
    pub created_at: idiolect_records::Datetime,
    /// Description of the persona's role, expertise, and information needs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Domain slug (fallback when domainUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub domain: Option<PersonaDomain>,
    /// AT-URI of the domain definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub domain_uri: Option<idiolect_records::AtUri>,
    /// Open-ended features: expertise level, certification, language proficiency, inter-annotator reliability, etc.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Annotation guidelines text.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub guidelines: Option<String>,
    /// Annotation guidelines document.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub guidelines_blob: Option<serde_json::Value>,
    /// Format of the guidelines text, so consumers can render it safely without sniffing. Defaults to plain when omitted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub guidelines_format: Option<PersonaGuidelinesFormat>,
    /// Persona kind slug (fallback when kindUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<PersonaKind>,
    /// AT-URI of the persona kind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind_uri: Option<idiolect_records::AtUri>,
    /// Knowledge graph references (e.g., ORCID, institutional identifiers, Wikidata for organizations).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// The persona name (e.g., 'Syntactician', 'Intelligence Analyst', 'Biomedical NER Annotator').
    pub name: String,
    /// Ontologies this persona uses for annotation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ontology_refs: Option<Vec<idiolect_records::AtUri>>,
    /// AT-URI of a parent persona this one specializes (e.g., 'Biomedical NER Annotator' specializes 'NER Annotator').
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_ref: Option<idiolect_records::AtUri>,
}

impl crate::Record for Persona {
    const NSID: &'static str = "pub.layers.persona.persona";
}

/// PersonaDomain. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum PersonaDomain {
    Linguistics,
    Nlp,
    Biomedical,
    Legal,
    Intelligence,
    SocialScience,
    Humanities,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl PersonaDomain {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Linguistics => "linguistics",
            Self::Nlp => "nlp",
            Self::Biomedical => "biomedical",
            Self::Legal => "legal",
            Self::Intelligence => "intelligence",
            Self::SocialScience => "social-science",
            Self::Humanities => "humanities",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for PersonaDomain {
    fn from(s: String) -> Self {
        match s.as_str() {
            "linguistics" => Self::Linguistics,
            "nlp" => Self::Nlp,
            "biomedical" => Self::Biomedical,
            "legal" => Self::Legal,
            "intelligence" => Self::Intelligence,
            "social-science" => Self::SocialScience,
            "humanities" => Self::Humanities,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for PersonaDomain {
    fn from(s: &str) -> Self {
        match s {
            "linguistics" => Self::Linguistics,
            "nlp" => Self::Nlp,
            "biomedical" => Self::Biomedical,
            "legal" => Self::Legal,
            "intelligence" => Self::Intelligence,
            "social-science" => Self::SocialScience,
            "humanities" => Self::Humanities,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for PersonaDomain {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for PersonaDomain {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// PersonaGuidelinesFormat. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum PersonaGuidelinesFormat {
    Plain,
    Html,
    Markdown,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl PersonaGuidelinesFormat {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Plain => "plain",
            Self::Html => "html",
            Self::Markdown => "markdown",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for PersonaGuidelinesFormat {
    fn from(s: String) -> Self {
        match s.as_str() {
            "plain" => Self::Plain,
            "html" => Self::Html,
            "markdown" => Self::Markdown,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for PersonaGuidelinesFormat {
    fn from(s: &str) -> Self {
        match s {
            "plain" => Self::Plain,
            "html" => Self::Html,
            "markdown" => Self::Markdown,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for PersonaGuidelinesFormat {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for PersonaGuidelinesFormat {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// PersonaKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum PersonaKind {
    HumanAnnotator,
    MlModel,
    GuidelinesPersona,
    ExpertPanel,
    CrowdWorker,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl PersonaKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::HumanAnnotator => "human-annotator",
            Self::MlModel => "ml-model",
            Self::GuidelinesPersona => "guidelines-persona",
            Self::ExpertPanel => "expert-panel",
            Self::CrowdWorker => "crowd-worker",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for PersonaKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "human-annotator" => Self::HumanAnnotator,
            "ml-model" => Self::MlModel,
            "guidelines-persona" => Self::GuidelinesPersona,
            "expert-panel" => Self::ExpertPanel,
            "crowd-worker" => Self::CrowdWorker,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for PersonaKind {
    fn from(s: &str) -> Self {
        match s {
            "human-annotator" => Self::HumanAnnotator,
            "ml-model" => Self::MlModel,
            "guidelines-persona" => Self::GuidelinesPersona,
            "expert-panel" => Self::ExpertPanel,
            "crowd-worker" => Self::CrowdWorker,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for PersonaKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for PersonaKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
