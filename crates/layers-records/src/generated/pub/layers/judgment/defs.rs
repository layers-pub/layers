// @generated by idiolect-codegen. do not edit.
// source: pub.layers.judgment.defs

//! Shared object definitions for the judgment namespace.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// Experiment design parameters for item distribution, ordering, and timing.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ExperimentDesign {
    /// How items are distributed to annotators.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub distribution_strategy: Option<ExperimentDesignDistributionStrategy>,
    /// AT-URI of the distribution strategy definition node. Community-expandable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub distribution_strategy_uri: Option<idiolect_records::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// How items are ordered within a list.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub item_order: Option<ExperimentDesignItemOrder>,
    /// AT-URI of the item order definition node. Community-expandable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub item_order_uri: Option<idiolect_records::AtUri>,
    /// Constraints on item list construction (Latin square balancing, no-adjacent-same-condition, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub list_constraints: Option<Vec<ListConstraint>>,
    /// Target timing per item in milliseconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timing_ms: Option<i64>,
}

/// A single judgment about a linguistic item.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Judgment {
    /// Behavioral signals: mouse movements, keystroke patterns, eye tracking data, scroll events, etc.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub behavioral_data: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Categorical judgment label.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub categorical_value: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub confidence: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Reference to the pub.layers.resource#filling that generated the item being judged.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filling_ref: Option<idiolect_records::AtUri>,
    /// Free-text response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub free_text: Option<String>,
    /// Reference to the item being judged. Use recordRef for the communication/annotation record, objectId for a specific object within it, localId for same-record references.
    pub item: crate::generated::r#pub::layers::defs::ObjectRef,
    /// Response time in milliseconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub response_time_ms: Option<i64>,
    /// Numeric response value (ordinal-scale rating, magnitude estimate, or rank position).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scalar_value: Option<i64>,
    /// Selected text span for span-labeling tasks.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub text_span: Option<crate::generated::r#pub::layers::defs::Span>,
}

/// A constraint on item list construction for an experiment.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ListConstraint {
    /// Optional formal constraint expression.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub constraint: Option<crate::generated::r#pub::layers::defs::Constraint>,
    /// Constraint kind slug.
    pub kind: ListConstraintKind,
    /// AT-URI of the constraint kind definition node. Community-expandable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind_uri: Option<idiolect_records::AtUri>,
    /// Constraint parameters (e.g., minimum distance, number of lists).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parameters: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// The item property this constraint operates on (e.g., 'condition', 'templateRef').
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target_property: Option<String>,
}

/// How stimuli are displayed to participants.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PresentationSpec {
    /// How text is segmented for incremental presentation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub chunking_unit: Option<PresentationSpecChunkingUnit>,
    /// Whether previous chunks remain visible during incremental presentation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cumulative: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Inter-stimulus interval in milliseconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub isi_ms: Option<i64>,
    /// Masking character for non-cumulative displays (e.g., '-', '#').
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mask_char: Option<String>,
    /// Presentation method (fallback when methodUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub method: Option<PresentationSpecMethod>,
    /// AT-URI of the presentation method definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub method_uri: Option<idiolect_records::AtUri>,
    /// Per-chunk display duration in milliseconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timing_ms: Option<i64>,
}

/// A data capture instrument used in an experiment.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct RecordingMethod {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Recording method (fallback when methodUri unavailable).
    pub method: RecordingMethodMethod,
    /// AT-URI of the recording method definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub method_uri: Option<idiolect_records::AtUri>,
}

/// ExperimentDesignDistributionStrategy. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ExperimentDesignDistributionStrategy {
    LatinSquare,
    Random,
    Blocked,
    Stratified,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl ExperimentDesignDistributionStrategy {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::LatinSquare => "latin-square",
            Self::Random => "random",
            Self::Blocked => "blocked",
            Self::Stratified => "stratified",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ExperimentDesignDistributionStrategy {
    fn from(s: String) -> Self {
        match s.as_str() {
            "latin-square" => Self::LatinSquare,
            "random" => Self::Random,
            "blocked" => Self::Blocked,
            "stratified" => Self::Stratified,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for ExperimentDesignDistributionStrategy {
    fn from(s: &str) -> Self {
        match s {
            "latin-square" => Self::LatinSquare,
            "random" => Self::Random,
            "blocked" => Self::Blocked,
            "stratified" => Self::Stratified,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for ExperimentDesignDistributionStrategy {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ExperimentDesignDistributionStrategy {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// ExperimentDesignItemOrder. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ExperimentDesignItemOrder {
    RandomOrder,
    FixedOrder,
    Blocked,
    Adaptive,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl ExperimentDesignItemOrder {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::RandomOrder => "random-order",
            Self::FixedOrder => "fixed-order",
            Self::Blocked => "blocked",
            Self::Adaptive => "adaptive",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ExperimentDesignItemOrder {
    fn from(s: String) -> Self {
        match s.as_str() {
            "random-order" => Self::RandomOrder,
            "fixed-order" => Self::FixedOrder,
            "blocked" => Self::Blocked,
            "adaptive" => Self::Adaptive,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for ExperimentDesignItemOrder {
    fn from(s: &str) -> Self {
        match s {
            "random-order" => Self::RandomOrder,
            "fixed-order" => Self::FixedOrder,
            "blocked" => Self::Blocked,
            "adaptive" => Self::Adaptive,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for ExperimentDesignItemOrder {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ExperimentDesignItemOrder {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// ListConstraintKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ListConstraintKind {
    LatinSquare,
    NoAdjacentSameCondition,
    BalancedFrequency,
    MinimumDistance,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl ListConstraintKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::LatinSquare => "latin-square",
            Self::NoAdjacentSameCondition => "no-adjacent-same-condition",
            Self::BalancedFrequency => "balanced-frequency",
            Self::MinimumDistance => "minimum-distance",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ListConstraintKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "latin-square" => Self::LatinSquare,
            "no-adjacent-same-condition" => Self::NoAdjacentSameCondition,
            "balanced-frequency" => Self::BalancedFrequency,
            "minimum-distance" => Self::MinimumDistance,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for ListConstraintKind {
    fn from(s: &str) -> Self {
        match s {
            "latin-square" => Self::LatinSquare,
            "no-adjacent-same-condition" => Self::NoAdjacentSameCondition,
            "balanced-frequency" => Self::BalancedFrequency,
            "minimum-distance" => Self::MinimumDistance,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for ListConstraintKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ListConstraintKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// PresentationSpecChunkingUnit. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum PresentationSpecChunkingUnit {
    Word,
    Character,
    Morpheme,
    Phrase,
    Sentence,
    Region,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl PresentationSpecChunkingUnit {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Word => "word",
            Self::Character => "character",
            Self::Morpheme => "morpheme",
            Self::Phrase => "phrase",
            Self::Sentence => "sentence",
            Self::Region => "region",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for PresentationSpecChunkingUnit {
    fn from(s: String) -> Self {
        match s.as_str() {
            "word" => Self::Word,
            "character" => Self::Character,
            "morpheme" => Self::Morpheme,
            "phrase" => Self::Phrase,
            "sentence" => Self::Sentence,
            "region" => Self::Region,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for PresentationSpecChunkingUnit {
    fn from(s: &str) -> Self {
        match s {
            "word" => Self::Word,
            "character" => Self::Character,
            "morpheme" => Self::Morpheme,
            "phrase" => Self::Phrase,
            "sentence" => Self::Sentence,
            "region" => Self::Region,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for PresentationSpecChunkingUnit {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for PresentationSpecChunkingUnit {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// PresentationSpecMethod. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum PresentationSpecMethod {
    Rsvp,
    SelfPaced,
    WholeSentence,
    Auditory,
    VisualWorld,
    MaskedPriming,
    CrossModal,
    Naturalistic,
    Gating,
    Maze,
    Boundary,
    MovingWindow,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl PresentationSpecMethod {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Rsvp => "rsvp",
            Self::SelfPaced => "self-paced",
            Self::WholeSentence => "whole-sentence",
            Self::Auditory => "auditory",
            Self::VisualWorld => "visual-world",
            Self::MaskedPriming => "masked-priming",
            Self::CrossModal => "cross-modal",
            Self::Naturalistic => "naturalistic",
            Self::Gating => "gating",
            Self::Maze => "maze",
            Self::Boundary => "boundary",
            Self::MovingWindow => "moving-window",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for PresentationSpecMethod {
    fn from(s: String) -> Self {
        match s.as_str() {
            "rsvp" => Self::Rsvp,
            "self-paced" => Self::SelfPaced,
            "whole-sentence" => Self::WholeSentence,
            "auditory" => Self::Auditory,
            "visual-world" => Self::VisualWorld,
            "masked-priming" => Self::MaskedPriming,
            "cross-modal" => Self::CrossModal,
            "naturalistic" => Self::Naturalistic,
            "gating" => Self::Gating,
            "maze" => Self::Maze,
            "boundary" => Self::Boundary,
            "moving-window" => Self::MovingWindow,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for PresentationSpecMethod {
    fn from(s: &str) -> Self {
        match s {
            "rsvp" => Self::Rsvp,
            "self-paced" => Self::SelfPaced,
            "whole-sentence" => Self::WholeSentence,
            "auditory" => Self::Auditory,
            "visual-world" => Self::VisualWorld,
            "masked-priming" => Self::MaskedPriming,
            "cross-modal" => Self::CrossModal,
            "naturalistic" => Self::Naturalistic,
            "gating" => Self::Gating,
            "maze" => Self::Maze,
            "boundary" => Self::Boundary,
            "moving-window" => Self::MovingWindow,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for PresentationSpecMethod {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for PresentationSpecMethod {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// RecordingMethodMethod. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum RecordingMethodMethod {
    ButtonBox,
    Keyboard,
    MouseClick,
    Touchscreen,
    Voice,
    Eeg,
    Meg,
    Fmri,
    Fnirs,
    EyeTracking,
    Pupillometry,
    MouseTracking,
    Emg,
    SkinConductance,
    Ecog,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl RecordingMethodMethod {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::ButtonBox => "button-box",
            Self::Keyboard => "keyboard",
            Self::MouseClick => "mouse-click",
            Self::Touchscreen => "touchscreen",
            Self::Voice => "voice",
            Self::Eeg => "eeg",
            Self::Meg => "meg",
            Self::Fmri => "fmri",
            Self::Fnirs => "fnirs",
            Self::EyeTracking => "eye-tracking",
            Self::Pupillometry => "pupillometry",
            Self::MouseTracking => "mouse-tracking",
            Self::Emg => "emg",
            Self::SkinConductance => "skin-conductance",
            Self::Ecog => "ecog",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for RecordingMethodMethod {
    fn from(s: String) -> Self {
        match s.as_str() {
            "button-box" => Self::ButtonBox,
            "keyboard" => Self::Keyboard,
            "mouse-click" => Self::MouseClick,
            "touchscreen" => Self::Touchscreen,
            "voice" => Self::Voice,
            "eeg" => Self::Eeg,
            "meg" => Self::Meg,
            "fmri" => Self::Fmri,
            "fnirs" => Self::Fnirs,
            "eye-tracking" => Self::EyeTracking,
            "pupillometry" => Self::Pupillometry,
            "mouse-tracking" => Self::MouseTracking,
            "emg" => Self::Emg,
            "skin-conductance" => Self::SkinConductance,
            "ecog" => Self::Ecog,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for RecordingMethodMethod {
    fn from(s: &str) -> Self {
        match s {
            "button-box" => Self::ButtonBox,
            "keyboard" => Self::Keyboard,
            "mouse-click" => Self::MouseClick,
            "touchscreen" => Self::Touchscreen,
            "voice" => Self::Voice,
            "eeg" => Self::Eeg,
            "meg" => Self::Meg,
            "fmri" => Self::Fmri,
            "fnirs" => Self::Fnirs,
            "eye-tracking" => Self::EyeTracking,
            "pupillometry" => Self::Pupillometry,
            "mouse-tracking" => Self::MouseTracking,
            "emg" => Self::Emg,
            "skin-conductance" => Self::SkinConductance,
            "ecog" => Self::Ecog,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for RecordingMethodMethod {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for RecordingMethodMethod {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
