// @generated by idiolect-codegen. do not edit.
// source: pub.layers.judgment.experimentDef

//! Generated from `pub.layers.judgment.experimentDef`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// Definition of an annotation or judgment experiment.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ExperimentDef {
    /// References to pub.layers.resource#collection records providing filler pools for this experiment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub collection_refs: Option<Vec<idiolect_records::AtUri>>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub corpus_ref: Option<idiolect_records::AtUri>,
    pub created_at: idiolect_records::Datetime,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Experiment design parameters: list constraints, distribution strategy, item order.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub design: Option<crate::generated::r#pub::layers::judgment::defs::ExperimentDesign>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub guidelines: Option<String>,
    /// Knowledge graph references (e.g., theoretical framework, methodology citation, task ontology).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// Available labels for categorical judgments.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub labels: Option<Vec<String>>,
    /// What property or behavior is being measured (fallback when measureTypeUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub measure_type: Option<ExperimentDefMeasureType>,
    /// AT-URI of the measure type definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub measure_type_uri: Option<idiolect_records::AtUri>,
    pub name: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ontology_ref: Option<idiolect_records::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub persona_ref: Option<idiolect_records::AtUri>,
    /// How stimuli are displayed to participants.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub presentation: Option<crate::generated::r#pub::layers::judgment::defs::PresentationSpec>,
    /// Data capture instruments used in this experiment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recording_methods:
        Option<Vec<crate::generated::r#pub::layers::judgment::defs::RecordingMethod>>,
    /// Maximum scale value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scale_max: Option<i64>,
    /// Minimum scale value for ordinal-scale judgments.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scale_min: Option<i64>,
    /// Response instrument: how the response is collected (fallback when taskTypeUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub task_type: Option<ExperimentDefTaskType>,
    /// AT-URI of the response instrument definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub task_type_uri: Option<idiolect_records::AtUri>,
    /// References to pub.layers.resource#template records used to generate stimuli for this experiment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub template_refs: Option<Vec<idiolect_records::AtUri>>,
}

impl crate::Record for ExperimentDef {
    const NSID: &'static str = "pub.layers.judgment.experimentDef";
}

/// ExperimentDefMeasureType. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ExperimentDefMeasureType {
    Acceptability,
    Inference,
    Similarity,
    Plausibility,
    Comprehension,
    Preference,
    Extraction,
    ReadingTime,
    Production,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl ExperimentDefMeasureType {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Acceptability => "acceptability",
            Self::Inference => "inference",
            Self::Similarity => "similarity",
            Self::Plausibility => "plausibility",
            Self::Comprehension => "comprehension",
            Self::Preference => "preference",
            Self::Extraction => "extraction",
            Self::ReadingTime => "reading-time",
            Self::Production => "production",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ExperimentDefMeasureType {
    fn from(s: String) -> Self {
        match s.as_str() {
            "acceptability" => Self::Acceptability,
            "inference" => Self::Inference,
            "similarity" => Self::Similarity,
            "plausibility" => Self::Plausibility,
            "comprehension" => Self::Comprehension,
            "preference" => Self::Preference,
            "extraction" => Self::Extraction,
            "reading-time" => Self::ReadingTime,
            "production" => Self::Production,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for ExperimentDefMeasureType {
    fn from(s: &str) -> Self {
        match s {
            "acceptability" => Self::Acceptability,
            "inference" => Self::Inference,
            "similarity" => Self::Similarity,
            "plausibility" => Self::Plausibility,
            "comprehension" => Self::Comprehension,
            "preference" => Self::Preference,
            "extraction" => Self::Extraction,
            "reading-time" => Self::ReadingTime,
            "production" => Self::Production,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for ExperimentDefMeasureType {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ExperimentDefMeasureType {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// ExperimentDefTaskType. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ExperimentDefTaskType {
    ForcedChoice,
    MultiSelect,
    OrdinalScale,
    Magnitude,
    Binary,
    Categorical,
    FreeText,
    Cloze,
    SpanLabeling,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl ExperimentDefTaskType {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::ForcedChoice => "forced-choice",
            Self::MultiSelect => "multi-select",
            Self::OrdinalScale => "ordinal-scale",
            Self::Magnitude => "magnitude",
            Self::Binary => "binary",
            Self::Categorical => "categorical",
            Self::FreeText => "free-text",
            Self::Cloze => "cloze",
            Self::SpanLabeling => "span-labeling",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ExperimentDefTaskType {
    fn from(s: String) -> Self {
        match s.as_str() {
            "forced-choice" => Self::ForcedChoice,
            "multi-select" => Self::MultiSelect,
            "ordinal-scale" => Self::OrdinalScale,
            "magnitude" => Self::Magnitude,
            "binary" => Self::Binary,
            "categorical" => Self::Categorical,
            "free-text" => Self::FreeText,
            "cloze" => Self::Cloze,
            "span-labeling" => Self::SpanLabeling,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for ExperimentDefTaskType {
    fn from(s: &str) -> Self {
        match s {
            "forced-choice" => Self::ForcedChoice,
            "multi-select" => Self::MultiSelect,
            "ordinal-scale" => Self::OrdinalScale,
            "magnitude" => Self::Magnitude,
            "binary" => Self::Binary,
            "categorical" => Self::Categorical,
            "free-text" => Self::FreeText,
            "cloze" => Self::Cloze,
            "span-labeling" => Self::SpanLabeling,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for ExperimentDefTaskType {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ExperimentDefTaskType {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
