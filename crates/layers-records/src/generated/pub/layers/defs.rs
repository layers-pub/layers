// @generated by idiolect-codegen. do not edit.
// source: pub.layers.defs

//! Shared definitions for the Layers lexicons. Provides abstract anchoring primitives, W3C Web Annotation-compatible selectors (for at.margin/Semble interoperability), alignment links, and universal metadata types.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A composable reference to any agent (human annotator, ML model, crowd worker, expert panel, etc.) that produced data. Separates the identity of the producer from the interpretive framework (persona) and the software used (tool). Consumers dispatch on which field(s) are populated: did for ATProto-native agents, id for anonymized or platform-specific identifiers, knowledgeRef for externally grounded agents (ORCID, HuggingFace model card, Wikidata).
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct AgentRef {
    /// ATProto DID of the agent, if the agent has one.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub did: Option<idiolect_records::Did>,
    /// Arbitrary string identifier (anonymized crowdworker ID, platform username, model version string, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// External knowledge graph reference for the agent (e.g., ORCID for a human, HuggingFace model card for an ML model, Wikidata for an organization).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_ref: Option<KnowledgeRef>,
    /// Human-readable display name for the agent.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

/// A single link in an alignment between two parallel sequences. Maps element(s) in a source sequence to element(s) in a target sequence. Supports many-to-many correspondence for interlinear glossing, parallel text alignment, cross-tokenization mapping, etc.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct AlignmentLink {
    /// Alignment confidence 0-1000.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub confidence: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<FeatureMap>,
    /// Knowledge graph references for this link (e.g., bilingual dictionary entry, translation memory source).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<KnowledgeRef>>,
    /// Optional label for the alignment link (e.g., alignment type).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub label: Option<String>,
    /// Indices into the source sequence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_indices: Option<Vec<i64>>,
    /// Indices into the target sequence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target_indices: Option<Vec<i64>>,
}

/// Abstract anchor: how an annotation attaches to its source data. This is a polymorphic type; at least one anchoring field should be present. Consumers dispatch on which field(s) are populated.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Anchor {
    /// External resource target (web page, document, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub external_target: Option<ExternalTarget>,
    /// Page and region in a paged document.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub page_anchor: Option<PageAnchor>,
    /// Spatio-temporal region in video.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spatio_temporal_anchor: Option<SpatioTemporalAnchor>,
    /// Temporal span in audio/video.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub temporal_span: Option<TemporalSpan>,
    /// Character-offset span in the expression text.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub text_span: Option<Span>,
    /// Single token reference.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_ref: Option<TokenRef>,
    /// Sequence of token references (possibly non-contiguous).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_ref_sequence: Option<TokenRefSequence>,
}

/// Metadata about who or what produced an annotation, when, and with what confidence. The three key provenance fields are: agent (who did it), personaRef (under what framework), and tool (with what software).
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct AnnotationMetadata {
    /// The agent (human or model) that produced this annotation. Distinct from personaRef (the interpretive framework) and tool (the software).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub agent: Option<AgentRef>,
    /// Confidence score scaled 0-1000 (to avoid floats). 1000 = maximum confidence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub confidence: Option<i64>,
    /// References to upstream records this annotation was derived from.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dependencies: Option<Vec<ObjectRef>>,
    /// Content hash for integrity verification.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub digest: Option<String>,
    /// Reference to the persona/annotation framework under which this annotation was produced. Distinct from agent (who did it).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub persona_ref: Option<idiolect_records::AtUri>,
    /// When the annotation was produced.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<idiolect_records::Datetime>,
    /// Name or identifier of the software tool used to produce this annotation (e.g., 'spaCy 3.7', 'brat 1.3', 'ELAN 6.4'). Distinct from agent (who ran the tool).
    pub tool: String,
}

/// A spatial bounding box for image or video frame annotation.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct BoundingBox {
    /// Height in pixels.
    pub height: i64,
    /// Width in pixels.
    pub width: i64,
    /// X coordinate of top-left corner in pixels.
    pub x: i64,
    /// Y coordinate of top-left corner in pixels.
    pub y: i64,
}

/// An abstract constraint expression. Used for type constraints on role slots, slot-level constraints in templates, cross-slot agreement constraints, and any other declarative restriction. The expression field holds a DSL string whose format is identified by expressionFormat/expressionFormatUri.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Constraint {
    /// Names of the slots or variables this constraint ranges over (for cross-slot and cross-template constraints).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub context: Option<Vec<String>>,
    /// Human-readable description of the constraint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// The constraint expression (e.g., 'self.pos == "VERB"', 'subject.features.number == verb.features.number').
    pub expression: String,
    /// Expression format slug (fallback when expressionFormatUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expression_format: Option<ConstraintExpressionFormat>,
    /// AT-URI of the expression format definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expression_format_uri: Option<idiolect_records::AtUri>,
    /// Constraint scope slug (fallback when scopeUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scope: Option<ConstraintScope>,
    /// AT-URI of the scope definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scope_uri: Option<idiolect_records::AtUri>,
}

/// Target for annotating external resources (web pages, documents, etc.). Compatible with at.margin's target model and the W3C Web Annotation Data Model.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ExternalTarget {
    /// W3C selector for identifying the specific segment within the resource.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub selector: Option<ExternalTargetSelector>,
    /// The URI of the external resource being annotated.
    pub source: idiolect_records::Uri,
    /// SHA256 hash of normalized URI for indexing.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_hash: Option<String>,
    /// Title of the resource at annotation time.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
}

/// A single key-value feature.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Feature {
    /// Feature name/key.
    pub key: String,
    /// Feature value as string. Consumers may parse typed values based on the key's semantics.
    pub value: String,
}

/// An open-ended set of typed key-value features that can be attached to any annotation. Provides maximum extensibility without committing to any label set or linguistic theory.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FeatureMap {
    /// The feature entries.
    pub entries: Vec<Feature>,
}

/// W3C FragmentSelector: selects by URI fragment identifier.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FragmentSelector {
    /// Specification the fragment conforms to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub conforms_to: Option<idiolect_records::Uri>,
    /// Fragment identifier value.
    pub value: String,
}

/// A spatial annotation at a specific time point.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Keyframe {
    pub bbox: BoundingBox,
    /// Per-keyframe features (e.g., visibility, occlusion percentage, confidence, pose data).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<FeatureMap>,
    /// Time in milliseconds.
    pub time_ms: i64,
}

/// A reference to an external knowledge base entry. Supports ATProto-native KBs (e.g., chive.pub with AT-URI nodes), external KBs (e.g., Wikidata with QIDs), and user/persona-specific KBs (AT-URIs in user PDSes).
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct KnowledgeRef {
    /// The identifier within the knowledge base (e.g., Wikidata QID, chive.pub node URI, Glottolog languoid ID).
    pub identifier: String,
    /// Human-readable label for the referenced entity.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub label: Option<String>,
    /// Knowledge base source slug (fallback when sourceUri unavailable).
    pub source: KnowledgeRefSource,
    /// AT-URI of the knowledge base type definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_uri: Option<idiolect_records::AtUri>,
    /// Optional full URI for the knowledge base entry.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uri: Option<idiolect_records::Uri>,
}

/// A composable reference to any Layers object, whether local (same record, by UUID), remote (different record, by AT-URI + optional object UUID), or external (knowledge graph entry). This is the universal cross-referencing primitive; consumers dispatch on which field(s) are populated. Used by argumentRef, graphNode, alignment endpoints, and any other cross-object pointer.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ObjectRef {
    /// Reference to an external knowledge graph node (Wikidata, chive.pub, FrameNet, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_ref: Option<KnowledgeRef>,
    /// UUID of an object within the same record.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub local_id: Option<Uuid>,
    /// UUID of a specific object within the record referenced by recordRef.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<Uuid>,
    /// AT-URI of a Layers record in another user's PDS or another record in the same PDS.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub record_ref: Option<idiolect_records::AtUri>,
}

/// Anchor to a specific page and region in a paged document (PDF, etc.). Compatible with chive.pub's page-level annotation model.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct PageAnchor {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bounding_box: Option<BoundingBox>,
    /// 0-indexed page number.
    pub page: i64,
    /// Character offsets within the page text.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub text_span: Option<Span>,
}

/// A contiguous span of text defined by UTF-8 byte offsets.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Span {
    /// Exclusive end UTF-8 byte offset.
    pub byte_end: i64,
    /// Inclusive start UTF-8 byte offset (0-indexed).
    pub byte_start: i64,
    /// Exclusive end character offset. Optional; for compatibility with character-offset datasets.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub char_end: Option<i64>,
    /// Inclusive start character offset. Optional; for compatibility with character-offset datasets.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub char_start: Option<i64>,
}

/// A normalized spatial value representing a point, region, line, or complex geometry. Parallel to temporalEntity. Subsumes GeoJSON geometry types, WKT primitives, and ISO 19107 spatial schema. Consumers dispatch on which fields are populated: bbox only (pixel bounding box), geometry+type (parsed geometry string), geometry+geometryFormat (format-specific parsing).
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SpatialEntity {
    /// Structured pixel bounding box (axis-aligned rectangle). The most common case for image/video annotation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bbox: Option<BoundingBox>,
    /// Coordinate reference system slug (fallback when crsUri unavailable). Determines how coordinates in geometry/bbox are interpreted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub crs: Option<SpatialEntityCrs>,
    /// AT-URI of the coordinate reference system definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub crs_uri: Option<idiolect_records::AtUri>,
    /// Number of coordinate dimensions (2 for planar, 3 for volumetric/elevation).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dimensions: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<FeatureMap>,
    /// Geometry as a string in the format specified by geometryFormat. WKT examples: 'POINT(37.7749 -122.4194)', 'POLYGON((0 0, 100 0, 100 100, 0 100, 0 0))'. GeoJSON example: '{"type":"Point","coordinates":[-122.4194,37.7749]}'. SVG path example: 'M 10 10 L 100 10 L 100 100 Z'. Default format is WKT.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub geometry: Option<String>,
    /// Format of the geometry string (fallback when geometryFormatUri unavailable). Default is WKT.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub geometry_format: Option<SpatialEntityGeometryFormat>,
    /// AT-URI of the geometry format definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub geometry_format_uri: Option<idiolect_records::AtUri>,
    /// Geometry type slug (fallback when typeUri unavailable). For dispatch without parsing the geometry string.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<SpatialEntityType>,
    /// AT-URI of the geometry type definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_uri: Option<idiolect_records::AtUri>,
    /// Spatial precision or uncertainty radius as a string with units (e.g., '50m', '10px', '0.001deg'). Units depend on the CRS.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uncertainty: Option<String>,
}

/// A complete spatial annotation packaging the expression type, normalized value, modifier, anchoring, and document function. Parallel to temporalExpression. Subsumes ISO-Space place annotations (ISO 24617-7), SpatialML PLACE elements, and general spatial semantic annotation. Attach to annotation objects via the spatial field.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SpatialExpression {
    /// What this spatial expression is relative to (e.g., a landmark annotation, a reference location, a trajector). For relative spatial expressions like 'behind the building'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub anchor_ref: Option<ObjectRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<FeatureMap>,
    /// Document function slug (fallback when functionUri unavailable). What role this place plays in the document.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub function: Option<SpatialExpressionFunction>,
    /// AT-URI of the document function definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub function_uri: Option<idiolect_records::AtUri>,
    /// Qualitative modifier (approximate, projected, interpolated, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modifier: Option<SpatialModifier>,
    /// Spatial expression type slug (fallback when typeUri unavailable). Maps to ISO-Space spatial entity types.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<SpatialExpressionType>,
    /// AT-URI of the spatial expression type definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_uri: Option<idiolect_records::AtUri>,
    /// The normalized spatial value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<SpatialEntity>,
}

/// Qualitative modification of a spatial value. Parallel to temporalModifier. Indicates precision, derivation method, or processing applied to a spatial entity.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SpatialModifier {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<FeatureMap>,
    /// Spatial modifier slug (fallback when modUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#mod: Option<SpatialModifierMod>,
    /// AT-URI of the spatial modifier definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mod_uri: Option<idiolect_records::AtUri>,
}

/// Combined spatial and temporal anchor for video annotation with keyframe-based tracking.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SpatioTemporalAnchor {
    /// Interpolation mode slug (fallback when interpolationUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interpolation: Option<SpatioTemporalAnchorInterpolation>,
    /// AT-URI of the interpolation mode definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interpolation_uri: Option<idiolect_records::AtUri>,
    /// Keyframes defining spatial positions at specific times.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub keyframes: Option<Vec<Keyframe>>,
    pub temporal_span: TemporalSpan,
}

/// A normalized temporal value representing a point, interval, duration, or uncertain range in calendar/clock time. Subsumes OWL-Time TemporalEntity (Instant, Interval, Duration) and TimeML TIMEX3 value. Consumers dispatch on which fields are populated: instant only (point), intervalStart+intervalEnd (bounded interval), duration only (pure duration), earliest+latest (uncertain bounds), recurrence (repeating pattern).
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TemporalEntity {
    /// Calendar system slug (fallback when calendarUri unavailable). Maps to OWL-Time TRS (Temporal Reference System).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub calendar: Option<TemporalEntityCalendar>,
    /// AT-URI of the calendar system definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub calendar_uri: Option<idiolect_records::AtUri>,
    /// Duration as ISO 8601 duration (e.g., 'P3Y', 'PT2H30M', 'P1DT12H'). Maps to OWL-Time hasTemporalDuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub duration: Option<String>,
    /// Lower bound for uncertain or vague times, as ISO 8601 datetime.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub earliest: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<FeatureMap>,
    /// Temporal granularity slug (fallback when granularityUri unavailable). Maps to OWL-Time unitType.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub granularity: Option<TemporalEntityGranularity>,
    /// AT-URI of the granularity definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub granularity_uri: Option<idiolect_records::AtUri>,
    /// Point in time as ISO 8601 datetime (e.g., '2024-03-15', '2024-03-15T14:30:00Z'). Maps to OWL-Time Instant.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instant: Option<String>,
    /// Interval end as ISO 8601 datetime. Maps to OWL-Time hasEnd.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interval_end: Option<String>,
    /// Interval start as ISO 8601 datetime. Maps to OWL-Time hasBeginning.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub interval_start: Option<String>,
    /// Upper bound for uncertain or vague times, as ISO 8601 datetime.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub latest: Option<String>,
    /// ISO 8601 repeating interval (e.g., 'R/P1W' for weekly, 'R5/P1D' for 5 daily repetitions).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recurrence: Option<String>,
}

/// A complete temporal annotation packaging the expression type, normalized value, modifier, anchoring, and document function. Subsumes TimeML TIMEX3 and OWL-Time GeneralDateTimeDescription. Attach to annotation objects via the temporal field.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TemporalExpression {
    /// What this temporal expression is relative to (e.g., document creation time, another temporal expression, a situation). Maps to TimeML anchorTimeID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub anchor_ref: Option<ObjectRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<FeatureMap>,
    /// Document function slug (fallback when functionUri unavailable). Maps to TimeML functionInDocument.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub function: Option<TemporalExpressionFunction>,
    /// AT-URI of the document function definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub function_uri: Option<idiolect_records::AtUri>,
    /// Qualitative modifier (approximate, early, late, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modifier: Option<TemporalModifier>,
    /// Temporal expression type slug (fallback when typeUri unavailable). Maps to TimeML TIMEX3 type.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<TemporalExpressionType>,
    /// AT-URI of the temporal expression type definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_uri: Option<idiolect_records::AtUri>,
    /// The normalized temporal value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<TemporalEntity>,
}

/// Qualitative modification of a temporal value. Subsumes TimeML TIMEX3 mod attribute and OWL-Time DateTimeDescription qualifiers.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TemporalModifier {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<FeatureMap>,
    /// Temporal modifier slug (fallback when modUri unavailable). Maps to TimeML TIMEX3 mod.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#mod: Option<TemporalModifierMod>,
    /// AT-URI of the temporal modifier definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mod_uri: Option<idiolect_records::AtUri>,
}

/// A temporal span within a media source, defined by start and end times in milliseconds.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TemporalSpan {
    /// End time in milliseconds.
    pub ending: i64,
    /// Start time in milliseconds.
    pub start: i64,
}

/// W3C TextPositionSelector adapted for ATProto: selects by UTF-8 byte offsets. Semantically equivalent to pub.layers.defs#span but named for W3C compatibility with at.margin.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TextPositionSelector {
    /// Ending UTF-8 byte position (exclusive).
    pub byte_end: i64,
    /// Starting UTF-8 byte position (0-indexed, inclusive).
    pub byte_start: i64,
    /// Ending character position (exclusive). Optional; for compatibility with character-offset datasets.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub char_end: Option<i64>,
    /// Starting character position (0-indexed, inclusive). Optional; for compatibility with character-offset datasets.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub char_start: Option<i64>,
}

/// W3C TextQuoteSelector: selects text by quoting it with surrounding context. Compatible with at.margin.annotation and the W3C Web Annotation Data Model.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TextQuoteSelector {
    /// The exact text to match.
    pub exact: String,
    /// Text immediately before the selection.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prefix: Option<String>,
    /// Text immediately after the selection.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub suffix: Option<String>,
}

/// A reference to a specific token within a tokenization, by index.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TokenRef {
    /// 0-based index of the token within its tokenization.
    pub token_index: i64,
    /// UUID of the tokenization containing the referenced token.
    pub tokenization_id: Uuid,
}

/// A sequence of token references, possibly non-contiguous, within a single tokenization.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TokenRefSequence {
    /// Optional head/anchor token index within the sequence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub anchor_token_index: Option<i64>,
    /// 0-based indices of the tokens.
    pub token_indexes: Vec<i64>,
    /// UUID of the tokenization containing the referenced tokens.
    pub tokenization_id: Uuid,
}

/// A universally unique identifier for cross-referencing annotation objects.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Uuid {
    /// The UUID string value.
    pub value: String,
}

/// ConstraintExpressionFormat. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ConstraintExpressionFormat {
    PythonExpr,
    JsonLogic,
    Regex,
    SparqlFilter,
    TypeRef,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl ConstraintExpressionFormat {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::PythonExpr => "python-expr",
            Self::JsonLogic => "json-logic",
            Self::Regex => "regex",
            Self::SparqlFilter => "sparql-filter",
            Self::TypeRef => "type-ref",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ConstraintExpressionFormat {
    fn from(s: String) -> Self {
        match s.as_str() {
            "python-expr" => Self::PythonExpr,
            "json-logic" => Self::JsonLogic,
            "regex" => Self::Regex,
            "sparql-filter" => Self::SparqlFilter,
            "type-ref" => Self::TypeRef,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for ConstraintExpressionFormat {
    fn from(s: &str) -> Self {
        match s {
            "python-expr" => Self::PythonExpr,
            "json-logic" => Self::JsonLogic,
            "regex" => Self::Regex,
            "sparql-filter" => Self::SparqlFilter,
            "type-ref" => Self::TypeRef,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for ConstraintExpressionFormat {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ConstraintExpressionFormat {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// ConstraintScope. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ConstraintScope {
    Slot,
    Template,
    CrossTemplate,
    Global,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl ConstraintScope {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Slot => "slot",
            Self::Template => "template",
            Self::CrossTemplate => "cross-template",
            Self::Global => "global",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ConstraintScope {
    fn from(s: String) -> Self {
        match s.as_str() {
            "slot" => Self::Slot,
            "template" => Self::Template,
            "cross-template" => Self::CrossTemplate,
            "global" => Self::Global,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for ConstraintScope {
    fn from(s: &str) -> Self {
        match s {
            "slot" => Self::Slot,
            "template" => Self::Template,
            "cross-template" => Self::CrossTemplate,
            "global" => Self::Global,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for ConstraintScope {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ConstraintScope {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// ExternalTargetSelector tagged union.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(tag = "$type")]
pub enum ExternalTargetSelector {
    #[serde(rename = "pub.layers.defs#textQuoteSelector")]
    TextQuoteSelector(TextQuoteSelector),
    #[serde(rename = "pub.layers.defs#textPositionSelector")]
    TextPositionSelector(TextPositionSelector),
    #[serde(rename = "pub.layers.defs#fragmentSelector")]
    FragmentSelector(FragmentSelector),
}

/// KnowledgeRefSource. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum KnowledgeRefSource {
    ChivePub,
    Wikidata,
    Wordnet,
    Framenet,
    Propbank,
    Verbnet,
    Unimorph,
    Glottolog,
    Cldr,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl KnowledgeRefSource {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::ChivePub => "chive.pub",
            Self::Wikidata => "wikidata",
            Self::Wordnet => "wordnet",
            Self::Framenet => "framenet",
            Self::Propbank => "propbank",
            Self::Verbnet => "verbnet",
            Self::Unimorph => "unimorph",
            Self::Glottolog => "glottolog",
            Self::Cldr => "cldr",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for KnowledgeRefSource {
    fn from(s: String) -> Self {
        match s.as_str() {
            "chive.pub" => Self::ChivePub,
            "wikidata" => Self::Wikidata,
            "wordnet" => Self::Wordnet,
            "framenet" => Self::Framenet,
            "propbank" => Self::Propbank,
            "verbnet" => Self::Verbnet,
            "unimorph" => Self::Unimorph,
            "glottolog" => Self::Glottolog,
            "cldr" => Self::Cldr,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for KnowledgeRefSource {
    fn from(s: &str) -> Self {
        match s {
            "chive.pub" => Self::ChivePub,
            "wikidata" => Self::Wikidata,
            "wordnet" => Self::Wordnet,
            "framenet" => Self::Framenet,
            "propbank" => Self::Propbank,
            "verbnet" => Self::Verbnet,
            "unimorph" => Self::Unimorph,
            "glottolog" => Self::Glottolog,
            "cldr" => Self::Cldr,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for KnowledgeRefSource {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for KnowledgeRefSource {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// SpatialEntityCrs. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum SpatialEntityCrs {
    Pixel,
    Percentage,
    Wgs84,
    WebMercator,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl SpatialEntityCrs {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Pixel => "pixel",
            Self::Percentage => "percentage",
            Self::Wgs84 => "wgs84",
            Self::WebMercator => "web-mercator",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for SpatialEntityCrs {
    fn from(s: String) -> Self {
        match s.as_str() {
            "pixel" => Self::Pixel,
            "percentage" => Self::Percentage,
            "wgs84" => Self::Wgs84,
            "web-mercator" => Self::WebMercator,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for SpatialEntityCrs {
    fn from(s: &str) -> Self {
        match s {
            "pixel" => Self::Pixel,
            "percentage" => Self::Percentage,
            "wgs84" => Self::Wgs84,
            "web-mercator" => Self::WebMercator,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for SpatialEntityCrs {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for SpatialEntityCrs {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// SpatialEntityGeometryFormat. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum SpatialEntityGeometryFormat {
    Wkt,
    Geojson,
    SvgPath,
    CocoPolygon,
    CocoRle,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl SpatialEntityGeometryFormat {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Wkt => "wkt",
            Self::Geojson => "geojson",
            Self::SvgPath => "svg-path",
            Self::CocoPolygon => "coco-polygon",
            Self::CocoRle => "coco-rle",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for SpatialEntityGeometryFormat {
    fn from(s: String) -> Self {
        match s.as_str() {
            "wkt" => Self::Wkt,
            "geojson" => Self::Geojson,
            "svg-path" => Self::SvgPath,
            "coco-polygon" => Self::CocoPolygon,
            "coco-rle" => Self::CocoRle,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for SpatialEntityGeometryFormat {
    fn from(s: &str) -> Self {
        match s {
            "wkt" => Self::Wkt,
            "geojson" => Self::Geojson,
            "svg-path" => Self::SvgPath,
            "coco-polygon" => Self::CocoPolygon,
            "coco-rle" => Self::CocoRle,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for SpatialEntityGeometryFormat {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for SpatialEntityGeometryFormat {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// SpatialEntityType. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum SpatialEntityType {
    Point,
    Box,
    Polygon,
    MultiPolygon,
    LineString,
    MultiLineString,
    Circle,
    Ellipse,
    MultiPoint,
    GeometryCollection,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl SpatialEntityType {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Point => "point",
            Self::Box => "box",
            Self::Polygon => "polygon",
            Self::MultiPolygon => "multi-polygon",
            Self::LineString => "line-string",
            Self::MultiLineString => "multi-line-string",
            Self::Circle => "circle",
            Self::Ellipse => "ellipse",
            Self::MultiPoint => "multi-point",
            Self::GeometryCollection => "geometry-collection",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for SpatialEntityType {
    fn from(s: String) -> Self {
        match s.as_str() {
            "point" => Self::Point,
            "box" => Self::Box,
            "polygon" => Self::Polygon,
            "multi-polygon" => Self::MultiPolygon,
            "line-string" => Self::LineString,
            "multi-line-string" => Self::MultiLineString,
            "circle" => Self::Circle,
            "ellipse" => Self::Ellipse,
            "multi-point" => Self::MultiPoint,
            "geometry-collection" => Self::GeometryCollection,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for SpatialEntityType {
    fn from(s: &str) -> Self {
        match s {
            "point" => Self::Point,
            "box" => Self::Box,
            "polygon" => Self::Polygon,
            "multi-polygon" => Self::MultiPolygon,
            "line-string" => Self::LineString,
            "multi-line-string" => Self::MultiLineString,
            "circle" => Self::Circle,
            "ellipse" => Self::Ellipse,
            "multi-point" => Self::MultiPoint,
            "geometry-collection" => Self::GeometryCollection,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for SpatialEntityType {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for SpatialEntityType {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// SpatialExpressionFunction. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum SpatialExpressionFunction {
    DocumentLocation,
    PublicationLocation,
    SituationLocation,
    Origin,
    Destination,
    Waypoint,
    None,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl SpatialExpressionFunction {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::DocumentLocation => "document-location",
            Self::PublicationLocation => "publication-location",
            Self::SituationLocation => "situation-location",
            Self::Origin => "origin",
            Self::Destination => "destination",
            Self::Waypoint => "waypoint",
            Self::None => "none",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for SpatialExpressionFunction {
    fn from(s: String) -> Self {
        match s.as_str() {
            "document-location" => Self::DocumentLocation,
            "publication-location" => Self::PublicationLocation,
            "situation-location" => Self::SituationLocation,
            "origin" => Self::Origin,
            "destination" => Self::Destination,
            "waypoint" => Self::Waypoint,
            "none" => Self::None,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for SpatialExpressionFunction {
    fn from(s: &str) -> Self {
        match s {
            "document-location" => Self::DocumentLocation,
            "publication-location" => Self::PublicationLocation,
            "situation-location" => Self::SituationLocation,
            "origin" => Self::Origin,
            "destination" => Self::Destination,
            "waypoint" => Self::Waypoint,
            "none" => Self::None,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for SpatialExpressionFunction {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for SpatialExpressionFunction {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// SpatialExpressionType. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum SpatialExpressionType {
    Location,
    Region,
    Path,
    Direction,
    Distance,
    Relative,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl SpatialExpressionType {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Location => "location",
            Self::Region => "region",
            Self::Path => "path",
            Self::Direction => "direction",
            Self::Distance => "distance",
            Self::Relative => "relative",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for SpatialExpressionType {
    fn from(s: String) -> Self {
        match s.as_str() {
            "location" => Self::Location,
            "region" => Self::Region,
            "path" => Self::Path,
            "direction" => Self::Direction,
            "distance" => Self::Distance,
            "relative" => Self::Relative,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for SpatialExpressionType {
    fn from(s: &str) -> Self {
        match s {
            "location" => Self::Location,
            "region" => Self::Region,
            "path" => Self::Path,
            "direction" => Self::Direction,
            "distance" => Self::Distance,
            "relative" => Self::Relative,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for SpatialExpressionType {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for SpatialExpressionType {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// SpatialModifierMod. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum SpatialModifierMod {
    Approximate,
    Projected,
    Interpolated,
    Estimated,
    Buffered,
    Simplified,
    Generalized,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl SpatialModifierMod {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Approximate => "approximate",
            Self::Projected => "projected",
            Self::Interpolated => "interpolated",
            Self::Estimated => "estimated",
            Self::Buffered => "buffered",
            Self::Simplified => "simplified",
            Self::Generalized => "generalized",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for SpatialModifierMod {
    fn from(s: String) -> Self {
        match s.as_str() {
            "approximate" => Self::Approximate,
            "projected" => Self::Projected,
            "interpolated" => Self::Interpolated,
            "estimated" => Self::Estimated,
            "buffered" => Self::Buffered,
            "simplified" => Self::Simplified,
            "generalized" => Self::Generalized,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for SpatialModifierMod {
    fn from(s: &str) -> Self {
        match s {
            "approximate" => Self::Approximate,
            "projected" => Self::Projected,
            "interpolated" => Self::Interpolated,
            "estimated" => Self::Estimated,
            "buffered" => Self::Buffered,
            "simplified" => Self::Simplified,
            "generalized" => Self::Generalized,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for SpatialModifierMod {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for SpatialModifierMod {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// SpatioTemporalAnchorInterpolation. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum SpatioTemporalAnchorInterpolation {
    Linear,
    Step,
    Cubic,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl SpatioTemporalAnchorInterpolation {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Linear => "linear",
            Self::Step => "step",
            Self::Cubic => "cubic",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for SpatioTemporalAnchorInterpolation {
    fn from(s: String) -> Self {
        match s.as_str() {
            "linear" => Self::Linear,
            "step" => Self::Step,
            "cubic" => Self::Cubic,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for SpatioTemporalAnchorInterpolation {
    fn from(s: &str) -> Self {
        match s {
            "linear" => Self::Linear,
            "step" => Self::Step,
            "cubic" => Self::Cubic,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for SpatioTemporalAnchorInterpolation {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for SpatioTemporalAnchorInterpolation {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// TemporalEntityCalendar. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum TemporalEntityCalendar {
    Gregorian,
    Julian,
    Hijri,
    Hebrew,
    IsoWeek,
    Unix,
    JapaneseImperial,
    Buddhist,
    Coptic,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl TemporalEntityCalendar {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Gregorian => "gregorian",
            Self::Julian => "julian",
            Self::Hijri => "hijri",
            Self::Hebrew => "hebrew",
            Self::IsoWeek => "iso-week",
            Self::Unix => "unix",
            Self::JapaneseImperial => "japanese-imperial",
            Self::Buddhist => "buddhist",
            Self::Coptic => "coptic",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for TemporalEntityCalendar {
    fn from(s: String) -> Self {
        match s.as_str() {
            "gregorian" => Self::Gregorian,
            "julian" => Self::Julian,
            "hijri" => Self::Hijri,
            "hebrew" => Self::Hebrew,
            "iso-week" => Self::IsoWeek,
            "unix" => Self::Unix,
            "japanese-imperial" => Self::JapaneseImperial,
            "buddhist" => Self::Buddhist,
            "coptic" => Self::Coptic,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for TemporalEntityCalendar {
    fn from(s: &str) -> Self {
        match s {
            "gregorian" => Self::Gregorian,
            "julian" => Self::Julian,
            "hijri" => Self::Hijri,
            "hebrew" => Self::Hebrew,
            "iso-week" => Self::IsoWeek,
            "unix" => Self::Unix,
            "japanese-imperial" => Self::JapaneseImperial,
            "buddhist" => Self::Buddhist,
            "coptic" => Self::Coptic,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for TemporalEntityCalendar {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for TemporalEntityCalendar {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// TemporalEntityGranularity. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum TemporalEntityGranularity {
    Millennium,
    Century,
    Decade,
    Year,
    Quarter,
    Month,
    Week,
    Day,
    Hour,
    Minute,
    Second,
    Millisecond,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl TemporalEntityGranularity {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Millennium => "millennium",
            Self::Century => "century",
            Self::Decade => "decade",
            Self::Year => "year",
            Self::Quarter => "quarter",
            Self::Month => "month",
            Self::Week => "week",
            Self::Day => "day",
            Self::Hour => "hour",
            Self::Minute => "minute",
            Self::Second => "second",
            Self::Millisecond => "millisecond",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for TemporalEntityGranularity {
    fn from(s: String) -> Self {
        match s.as_str() {
            "millennium" => Self::Millennium,
            "century" => Self::Century,
            "decade" => Self::Decade,
            "year" => Self::Year,
            "quarter" => Self::Quarter,
            "month" => Self::Month,
            "week" => Self::Week,
            "day" => Self::Day,
            "hour" => Self::Hour,
            "minute" => Self::Minute,
            "second" => Self::Second,
            "millisecond" => Self::Millisecond,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for TemporalEntityGranularity {
    fn from(s: &str) -> Self {
        match s {
            "millennium" => Self::Millennium,
            "century" => Self::Century,
            "decade" => Self::Decade,
            "year" => Self::Year,
            "quarter" => Self::Quarter,
            "month" => Self::Month,
            "week" => Self::Week,
            "day" => Self::Day,
            "hour" => Self::Hour,
            "minute" => Self::Minute,
            "second" => Self::Second,
            "millisecond" => Self::Millisecond,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for TemporalEntityGranularity {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for TemporalEntityGranularity {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// TemporalExpressionFunction. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum TemporalExpressionFunction {
    CreationTime,
    PublicationTime,
    ExpirationTime,
    ModificationTime,
    ReleaseTime,
    ReceptionTime,
    None,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl TemporalExpressionFunction {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::CreationTime => "creation-time",
            Self::PublicationTime => "publication-time",
            Self::ExpirationTime => "expiration-time",
            Self::ModificationTime => "modification-time",
            Self::ReleaseTime => "release-time",
            Self::ReceptionTime => "reception-time",
            Self::None => "none",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for TemporalExpressionFunction {
    fn from(s: String) -> Self {
        match s.as_str() {
            "creation-time" => Self::CreationTime,
            "publication-time" => Self::PublicationTime,
            "expiration-time" => Self::ExpirationTime,
            "modification-time" => Self::ModificationTime,
            "release-time" => Self::ReleaseTime,
            "reception-time" => Self::ReceptionTime,
            "none" => Self::None,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for TemporalExpressionFunction {
    fn from(s: &str) -> Self {
        match s {
            "creation-time" => Self::CreationTime,
            "publication-time" => Self::PublicationTime,
            "expiration-time" => Self::ExpirationTime,
            "modification-time" => Self::ModificationTime,
            "release-time" => Self::ReleaseTime,
            "reception-time" => Self::ReceptionTime,
            "none" => Self::None,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for TemporalExpressionFunction {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for TemporalExpressionFunction {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// TemporalExpressionType. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum TemporalExpressionType {
    Date,
    Time,
    Duration,
    Set,
    Interval,
    Relative,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl TemporalExpressionType {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Date => "date",
            Self::Time => "time",
            Self::Duration => "duration",
            Self::Set => "set",
            Self::Interval => "interval",
            Self::Relative => "relative",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for TemporalExpressionType {
    fn from(s: String) -> Self {
        match s.as_str() {
            "date" => Self::Date,
            "time" => Self::Time,
            "duration" => Self::Duration,
            "set" => Self::Set,
            "interval" => Self::Interval,
            "relative" => Self::Relative,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for TemporalExpressionType {
    fn from(s: &str) -> Self {
        match s {
            "date" => Self::Date,
            "time" => Self::Time,
            "duration" => Self::Duration,
            "set" => Self::Set,
            "interval" => Self::Interval,
            "relative" => Self::Relative,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for TemporalExpressionType {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for TemporalExpressionType {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// TemporalModifierMod. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum TemporalModifierMod {
    Approximate,
    Early,
    Mid,
    Late,
    Start,
    End,
    Before,
    After,
    OnOrBefore,
    OnOrAfter,
    LessThan,
    MoreThan,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl TemporalModifierMod {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Approximate => "approximate",
            Self::Early => "early",
            Self::Mid => "mid",
            Self::Late => "late",
            Self::Start => "start",
            Self::End => "end",
            Self::Before => "before",
            Self::After => "after",
            Self::OnOrBefore => "on-or-before",
            Self::OnOrAfter => "on-or-after",
            Self::LessThan => "less-than",
            Self::MoreThan => "more-than",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for TemporalModifierMod {
    fn from(s: String) -> Self {
        match s.as_str() {
            "approximate" => Self::Approximate,
            "early" => Self::Early,
            "mid" => Self::Mid,
            "late" => Self::Late,
            "start" => Self::Start,
            "end" => Self::End,
            "before" => Self::Before,
            "after" => Self::After,
            "on-or-before" => Self::OnOrBefore,
            "on-or-after" => Self::OnOrAfter,
            "less-than" => Self::LessThan,
            "more-than" => Self::MoreThan,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for TemporalModifierMod {
    fn from(s: &str) -> Self {
        match s {
            "approximate" => Self::Approximate,
            "early" => Self::Early,
            "mid" => Self::Mid,
            "late" => Self::Late,
            "start" => Self::Start,
            "end" => Self::End,
            "before" => Self::Before,
            "after" => Self::After,
            "on-or-before" => Self::OnOrBefore,
            "on-or-after" => Self::OnOrAfter,
            "less-than" => Self::LessThan,
            "more-than" => Self::MoreThan,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for TemporalModifierMod {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for TemporalModifierMod {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
