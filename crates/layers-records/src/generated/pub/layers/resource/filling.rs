// @generated by idiolect-codegen. do not edit.
// source: pub.layers.resource.filling

//! Generated from `pub.layers.resource.filling`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A filled template: a template with all slots mapped to specific fillers, producing a rendered text. Generalizes bead's FilledTemplate and Item. The rendered text can optionally be materialized as a pub.layers.expression for annotation. Fillings are composable: they reference templates, entries, and communications via AT-URIs.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Filling {
    pub created_at: idiolect_records::Datetime,
    /// AT-URI of the pub.layers.expression materializing this filling (for annotation).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expression_ref: Option<idiolect_records::AtUri>,
    /// Open-ended features: model name for MLM filling, seed for random, solver stats for CSP, etc.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Knowledge graph references (e.g., generation model in a KB, sampling distribution, linguistic theory motivating the filling).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// Provenance: what tool/process generated this filling.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<crate::generated::r#pub::layers::defs::AnnotationMetadata>,
    /// The fully rendered text after substitution.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rendered_text: Option<String>,
    /// The slot→filler mappings.
    pub slot_fillings: Vec<crate::generated::r#pub::layers::resource::defs::SlotFilling>,
    /// Filling strategy slug (fallback when strategyUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub strategy: Option<FillingStrategy>,
    /// AT-URI of the filling strategy definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub strategy_uri: Option<idiolect_records::AtUri>,
    /// AT-URI of the template being filled.
    pub template_ref: idiolect_records::AtUri,
}

impl crate::Record for Filling {
    const NSID: &'static str = "pub.layers.resource.filling";
}

/// FillingStrategy. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum FillingStrategy {
    Exhaustive,
    Random,
    Stratified,
    Mlm,
    Csp,
    Mixed,
    Manual,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl FillingStrategy {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Exhaustive => "exhaustive",
            Self::Random => "random",
            Self::Stratified => "stratified",
            Self::Mlm => "mlm",
            Self::Csp => "csp",
            Self::Mixed => "mixed",
            Self::Manual => "manual",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for FillingStrategy {
    fn from(s: String) -> Self {
        match s.as_str() {
            "exhaustive" => Self::Exhaustive,
            "random" => Self::Random,
            "stratified" => Self::Stratified,
            "mlm" => Self::Mlm,
            "csp" => Self::Csp,
            "mixed" => Self::Mixed,
            "manual" => Self::Manual,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for FillingStrategy {
    fn from(s: &str) -> Self {
        match s {
            "exhaustive" => Self::Exhaustive,
            "random" => Self::Random,
            "stratified" => Self::Stratified,
            "mlm" => Self::Mlm,
            "csp" => Self::Csp,
            "mixed" => Self::Mixed,
            "manual" => Self::Manual,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for FillingStrategy {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for FillingStrategy {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
