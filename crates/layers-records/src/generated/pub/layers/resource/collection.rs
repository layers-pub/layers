// @generated by idiolect-codegen. do not edit.
// source: pub.layers.resource.collection

//! Generated from `pub.layers.resource.collection`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A named collection of linguistic resource entries. Abstract enough to represent bead Lexicons, FrameNet frame inventories, PropBank frame files, WordNet synsets, morphological paradigm tables, gazetteers, stop-word lists, etc.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Collection {
    pub created_at: idiolect_records::Datetime,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Collection kind slug (fallback when kindUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<CollectionKind>,
    /// AT-URI of the collection kind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind_uri: Option<idiolect_records::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// BCP-47 language tags this record covers. Empty when language is unspecified or unknown.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub languages: Option<Vec<String>>,
    /// Provenance: who curated this collection, with what tool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<crate::generated::r#pub::layers::defs::AnnotationMetadata>,
    /// Human-readable name for this collection.
    pub name: String,
    /// Reference to a pub.layers.ontology defining the type system for entries in this collection.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ontology_ref: Option<idiolect_records::AtUri>,
    /// Version string (e.g., 'FrameNet 1.7', 'PropBank 3.4').
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version: Option<String>,
}

impl crate::Record for Collection {
    const NSID: &'static str = "pub.layers.resource.collection";
}

/// CollectionKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum CollectionKind {
    Lexicon,
    FrameInventory,
    Gazetteer,
    Paradigm,
    StopList,
    StimulusPool,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl CollectionKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Lexicon => "lexicon",
            Self::FrameInventory => "frame-inventory",
            Self::Gazetteer => "gazetteer",
            Self::Paradigm => "paradigm",
            Self::StopList => "stop-list",
            Self::StimulusPool => "stimulus-pool",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for CollectionKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "lexicon" => Self::Lexicon,
            "frame-inventory" => Self::FrameInventory,
            "gazetteer" => Self::Gazetteer,
            "paradigm" => Self::Paradigm,
            "stop-list" => Self::StopList,
            "stimulus-pool" => Self::StimulusPool,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for CollectionKind {
    fn from(s: &str) -> Self {
        match s {
            "lexicon" => Self::Lexicon,
            "frame-inventory" => Self::FrameInventory,
            "gazetteer" => Self::Gazetteer,
            "paradigm" => Self::Paradigm,
            "stop-list" => Self::StopList,
            "stimulus-pool" => Self::StimulusPool,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for CollectionKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for CollectionKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
