// @generated by idiolect-codegen. do not edit.
// source: pub.layers.resource.entry

//! Generated from `pub.layers.resource.entry`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A linguistic resource entry: a lexical item, frame element filler, morphological paradigm cell, or any atomic unit in a structured linguistic collection. Abstract enough to represent bead LexicalItems, FrameNet lexical units, PropBank rolesets, WordNet synset members, morphological paradigm cells, etc.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Entry {
    /// For multi-word expressions: the component words.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub components: Option<Vec<crate::generated::r#pub::layers::resource::defs::MweComponent>>,
    pub created_at: idiolect_records::Datetime,
    /// Open-ended features: pos, morphological features, frequency, register, etc.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Surface form or string representation.
    pub form: String,
    /// Knowledge graph groundings (WordNet synset, FrameNet lexical unit, Wikidata, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// BCP-47 language tag.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language: Option<String>,
    /// Canonical/citation form.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lemma: Option<String>,
    /// Provenance: who created this entry, with what tool, under what persona.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<crate::generated::r#pub::layers::defs::AnnotationMetadata>,
    /// MWE kind slug (fallback when mweKindUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mwe_kind: Option<EntryMweKind>,
    /// AT-URI of the MWE kind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mwe_kind_uri: Option<idiolect_records::AtUri>,
    /// Reference to a pub.layers.ontology#typeDef classifying this entry.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ontology_type_ref: Option<idiolect_records::AtUri>,
    /// AT-URI of the source record this entry was derived from (e.g., an annotation, another entry).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_ref: Option<idiolect_records::AtUri>,
}

impl crate::Record for Entry {
    const NSID: &'static str = "pub.layers.resource.entry";
}

/// EntryMweKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum EntryMweKind {
    Compound,
    PhrasalVerb,
    Idiom,
    LightVerb,
    NamedEntity,
    Collocation,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl EntryMweKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Compound => "compound",
            Self::PhrasalVerb => "phrasal-verb",
            Self::Idiom => "idiom",
            Self::LightVerb => "light-verb",
            Self::NamedEntity => "named-entity",
            Self::Collocation => "collocation",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for EntryMweKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "compound" => Self::Compound,
            "phrasal-verb" => Self::PhrasalVerb,
            "idiom" => Self::Idiom,
            "light-verb" => Self::LightVerb,
            "named-entity" => Self::NamedEntity,
            "collocation" => Self::Collocation,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for EntryMweKind {
    fn from(s: &str) -> Self {
        match s {
            "compound" => Self::Compound,
            "phrasal-verb" => Self::PhrasalVerb,
            "idiom" => Self::Idiom,
            "light-verb" => Self::LightVerb,
            "named-entity" => Self::NamedEntity,
            "collocation" => Self::Collocation,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for EntryMweKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for EntryMweKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
