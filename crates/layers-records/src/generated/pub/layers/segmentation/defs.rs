// @generated by idiolect-codegen. do not edit.
// source: pub.layers.segmentation.defs

//! Generated from `pub.layers.segmentation.defs`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A single token within a tokenization.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Token {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub temporal_span: Option<crate::generated::r#pub::layers::defs::TemporalSpan>,
    /// The surface form of the token.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub text: Option<String>,
    /// UTF-8 byte offsets into the expression text.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub text_span: Option<crate::generated::r#pub::layers::defs::Span>,
    /// Position of this token in the tokenization (0-based).
    pub token_index: i64,
}

/// An ordered sequence of tokens for an expression or sub-expression. Multiple tokenizations can coexist for the same expression (e.g., whitespace vs. BPE vs. morphological), enabling interlinear glossing, alternative segmentation strategies, or multi-granularity analysis.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Tokenization {
    /// Reference to the specific sub-expression this tokenization covers (e.g., a sentence-level expression). If absent, covers the entire expression referenced by the segmentation record.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expression_ref: Option<idiolect_records::AtUri>,
    /// Tokenization kind slug (fallback when kindUri unavailable).
    pub kind: TokenizationKind,
    /// AT-URI of the tokenization kind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind_uri: Option<idiolect_records::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<crate::generated::r#pub::layers::defs::AnnotationMetadata>,
    /// The ordered token sequence.
    pub tokens: Vec<Token>,
    pub uuid: crate::generated::r#pub::layers::defs::Uuid,
}

/// TokenizationKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum TokenizationKind {
    Whitespace,
    PennTreebank,
    Bpe,
    Sentencepiece,
    Character,
    Morphological,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl TokenizationKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Whitespace => "whitespace",
            Self::PennTreebank => "penn-treebank",
            Self::Bpe => "bpe",
            Self::Sentencepiece => "sentencepiece",
            Self::Character => "character",
            Self::Morphological => "morphological",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for TokenizationKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "whitespace" => Self::Whitespace,
            "penn-treebank" => Self::PennTreebank,
            "bpe" => Self::Bpe,
            "sentencepiece" => Self::Sentencepiece,
            "character" => Self::Character,
            "morphological" => Self::Morphological,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for TokenizationKind {
    fn from(s: &str) -> Self {
        match s {
            "whitespace" => Self::Whitespace,
            "penn-treebank" => Self::PennTreebank,
            "bpe" => Self::Bpe,
            "sentencepiece" => Self::Sentencepiece,
            "character" => Self::Character,
            "morphological" => Self::Morphological,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for TokenizationKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for TokenizationKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
