// @generated by idiolect-codegen. do not edit.
// source: pub.layers.graph.graphNode

//! Generated from `pub.layers.graph.graphNode`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A standalone node in the property graph. Represents entities, concepts, situations, claims, or any domain object that does not have another Layers record. Existing Layers records (expressions, annotations, typeDefs) are implicitly nodes via objectRef.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct GraphNode {
    pub created_at: idiolect_records::Datetime,
    /// Knowledge graph references grounding this node (Wikidata, chive.pub, FrameNet, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// Human-readable node label.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub label: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<crate::generated::r#pub::layers::defs::AnnotationMetadata>,
    /// Node type slug (fallback when nodeTypeUri unavailable).
    pub node_type: GraphNodeNodeType,
    /// AT-URI of the node type definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_uri: Option<idiolect_records::AtUri>,
    /// Node properties as key-value pairs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub properties: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
}

impl crate::Record for GraphNode {
    const NSID: &'static str = "pub.layers.graph.graphNode";
}

/// GraphNodeNodeType. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum GraphNodeNodeType {
    Entity,
    Concept,
    Situation,
    State,
    Time,
    Location,
    Claim,
    Proposition,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl GraphNodeNodeType {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Entity => "entity",
            Self::Concept => "concept",
            Self::Situation => "situation",
            Self::State => "state",
            Self::Time => "time",
            Self::Location => "location",
            Self::Claim => "claim",
            Self::Proposition => "proposition",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for GraphNodeNodeType {
    fn from(s: String) -> Self {
        match s.as_str() {
            "entity" => Self::Entity,
            "concept" => Self::Concept,
            "situation" => Self::Situation,
            "state" => Self::State,
            "time" => Self::Time,
            "location" => Self::Location,
            "claim" => Self::Claim,
            "proposition" => Self::Proposition,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for GraphNodeNodeType {
    fn from(s: &str) -> Self {
        match s {
            "entity" => Self::Entity,
            "concept" => Self::Concept,
            "situation" => Self::Situation,
            "state" => Self::State,
            "time" => Self::Time,
            "location" => Self::Location,
            "claim" => Self::Claim,
            "proposition" => Self::Proposition,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for GraphNodeNodeType {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for GraphNodeNodeType {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
