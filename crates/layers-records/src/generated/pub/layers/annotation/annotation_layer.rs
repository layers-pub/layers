// @generated by idiolect-codegen. do not edit.
// source: pub.layers.annotation.annotationLayer

//! Generated from `pub.layers.annotation.annotationLayer`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A named layer of annotations over an expression. All annotation types use this single record type. The combination of kind, subkind, and formalism tells the appview how to render. Multiple layers can coexist for the same expression.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct AnnotationLayer {
    /// Reference to the top-ranked layer in a k-best group.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alternatives_ref: Option<idiolect_records::AtUri>,
    /// The annotations in this layer.
    pub annotations: Vec<crate::generated::r#pub::layers::annotation::defs::Annotation>,
    pub created_at: idiolect_records::Datetime,
    /// The expression this annotation layer applies to.
    pub expression: idiolect_records::AtUri,
    /// Formalism slug (fallback when formalismUri unavailable). The linguistic formalism or annotation standard used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub formalism: Option<AnnotationLayerFormalism>,
    /// AT-URI of the formalism definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub formalism_uri: Option<idiolect_records::AtUri>,
    /// Primary annotation kind slug (fallback when kindUri unavailable). Determines the structural interpretation of annotations in this layer.
    pub kind: AnnotationLayerKind,
    /// AT-URI of the annotation kind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind_uri: Option<idiolect_records::AtUri>,
    /// Identifier for the label set used (e.g., 'universal-pos', 'ontonotes-ner', 'penn-treebank-pos').
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub label_set: Option<String>,
    /// BCP-47 language tag for this annotation layer, if different from the expression's language.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<crate::generated::r#pub::layers::defs::AnnotationMetadata>,
    /// Reference to a pub.layers.ontology defining the types used in this layer.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ontology_ref: Option<idiolect_records::AtUri>,
    /// For dependent/subordinate layers: the parent layer this one subdivides or refines. Supports ELAN-style tier dependencies, error-correction pairs, etc.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_layer_ref: Option<idiolect_records::AtUri>,
    /// Rank among k-best alternatives (1 = best).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rank: Option<i64>,
    /// How this annotation layer was produced (fallback when sourceMethodUri unavailable). Follows UD's per-layer annotation source tracking.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_method: Option<AnnotationLayerSourceMethod>,
    /// AT-URI of the annotation source method definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_method_uri: Option<idiolect_records::AtUri>,
    /// Annotation subkind slug (fallback when subkindUri unavailable). The appview uses this for specialized rendering.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subkind: Option<AnnotationLayerSubkind>,
    /// AT-URI of the annotation subkind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subkind_uri: Option<idiolect_records::AtUri>,
    /// For token-aligned layers (kind=token-tag, dependency, constituency, etc.): the tokenization these annotations are aligned to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tokenization_id: Option<crate::generated::r#pub::layers::defs::Uuid>,
}

impl crate::Record for AnnotationLayer {
    const NSID: &'static str = "pub.layers.annotation.annotationLayer";
}

/// AnnotationLayerFormalism. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum AnnotationLayerFormalism {
    UniversalDependencies,
    PennTreebank,
    Stanford,
    Prague,
    Propbank,
    Framenet,
    Verbnet,
    Amr,
    Ucca,
    Rst,
    Erst,
    Sdrt,
    Pdtb,
    Timeml,
    IsoSpace,
    Spatialml,
    ConllU,
    Brat,
    Elan,
    LeipzigGlossing,
    Ipa,
    Tobi,
    Bpe,
    Sentencepiece,
    Unimorph,
    Wals,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl AnnotationLayerFormalism {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::UniversalDependencies => "universal-dependencies",
            Self::PennTreebank => "penn-treebank",
            Self::Stanford => "stanford",
            Self::Prague => "prague",
            Self::Propbank => "propbank",
            Self::Framenet => "framenet",
            Self::Verbnet => "verbnet",
            Self::Amr => "amr",
            Self::Ucca => "ucca",
            Self::Rst => "rst",
            Self::Erst => "erst",
            Self::Sdrt => "sdrt",
            Self::Pdtb => "pdtb",
            Self::Timeml => "timeml",
            Self::IsoSpace => "iso-space",
            Self::Spatialml => "spatialml",
            Self::ConllU => "conll-u",
            Self::Brat => "brat",
            Self::Elan => "elan",
            Self::LeipzigGlossing => "leipzig-glossing",
            Self::Ipa => "ipa",
            Self::Tobi => "tobi",
            Self::Bpe => "bpe",
            Self::Sentencepiece => "sentencepiece",
            Self::Unimorph => "unimorph",
            Self::Wals => "wals",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for AnnotationLayerFormalism {
    fn from(s: String) -> Self {
        match s.as_str() {
            "universal-dependencies" => Self::UniversalDependencies,
            "penn-treebank" => Self::PennTreebank,
            "stanford" => Self::Stanford,
            "prague" => Self::Prague,
            "propbank" => Self::Propbank,
            "framenet" => Self::Framenet,
            "verbnet" => Self::Verbnet,
            "amr" => Self::Amr,
            "ucca" => Self::Ucca,
            "rst" => Self::Rst,
            "erst" => Self::Erst,
            "sdrt" => Self::Sdrt,
            "pdtb" => Self::Pdtb,
            "timeml" => Self::Timeml,
            "iso-space" => Self::IsoSpace,
            "spatialml" => Self::Spatialml,
            "conll-u" => Self::ConllU,
            "brat" => Self::Brat,
            "elan" => Self::Elan,
            "leipzig-glossing" => Self::LeipzigGlossing,
            "ipa" => Self::Ipa,
            "tobi" => Self::Tobi,
            "bpe" => Self::Bpe,
            "sentencepiece" => Self::Sentencepiece,
            "unimorph" => Self::Unimorph,
            "wals" => Self::Wals,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for AnnotationLayerFormalism {
    fn from(s: &str) -> Self {
        match s {
            "universal-dependencies" => Self::UniversalDependencies,
            "penn-treebank" => Self::PennTreebank,
            "stanford" => Self::Stanford,
            "prague" => Self::Prague,
            "propbank" => Self::Propbank,
            "framenet" => Self::Framenet,
            "verbnet" => Self::Verbnet,
            "amr" => Self::Amr,
            "ucca" => Self::Ucca,
            "rst" => Self::Rst,
            "erst" => Self::Erst,
            "sdrt" => Self::Sdrt,
            "pdtb" => Self::Pdtb,
            "timeml" => Self::Timeml,
            "iso-space" => Self::IsoSpace,
            "spatialml" => Self::Spatialml,
            "conll-u" => Self::ConllU,
            "brat" => Self::Brat,
            "elan" => Self::Elan,
            "leipzig-glossing" => Self::LeipzigGlossing,
            "ipa" => Self::Ipa,
            "tobi" => Self::Tobi,
            "bpe" => Self::Bpe,
            "sentencepiece" => Self::Sentencepiece,
            "unimorph" => Self::Unimorph,
            "wals" => Self::Wals,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for AnnotationLayerFormalism {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for AnnotationLayerFormalism {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// AnnotationLayerKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum AnnotationLayerKind {
    TokenTag,
    Span,
    Relation,
    Tree,
    Graph,
    Tier,
    DocumentTag,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl AnnotationLayerKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::TokenTag => "token-tag",
            Self::Span => "span",
            Self::Relation => "relation",
            Self::Tree => "tree",
            Self::Graph => "graph",
            Self::Tier => "tier",
            Self::DocumentTag => "document-tag",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for AnnotationLayerKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "token-tag" => Self::TokenTag,
            "span" => Self::Span,
            "relation" => Self::Relation,
            "tree" => Self::Tree,
            "graph" => Self::Graph,
            "tier" => Self::Tier,
            "document-tag" => Self::DocumentTag,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for AnnotationLayerKind {
    fn from(s: &str) -> Self {
        match s {
            "token-tag" => Self::TokenTag,
            "span" => Self::Span,
            "relation" => Self::Relation,
            "tree" => Self::Tree,
            "graph" => Self::Graph,
            "tier" => Self::Tier,
            "document-tag" => Self::DocumentTag,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for AnnotationLayerKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for AnnotationLayerKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// AnnotationLayerSourceMethod. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum AnnotationLayerSourceMethod {
    ManualNative,
    ManualCorrected,
    Automatic,
    AutomaticCorrected,
    Converted,
    ConvertedCorrected,
    CrowdSourced,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl AnnotationLayerSourceMethod {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::ManualNative => "manual-native",
            Self::ManualCorrected => "manual-corrected",
            Self::Automatic => "automatic",
            Self::AutomaticCorrected => "automatic-corrected",
            Self::Converted => "converted",
            Self::ConvertedCorrected => "converted-corrected",
            Self::CrowdSourced => "crowd-sourced",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for AnnotationLayerSourceMethod {
    fn from(s: String) -> Self {
        match s.as_str() {
            "manual-native" => Self::ManualNative,
            "manual-corrected" => Self::ManualCorrected,
            "automatic" => Self::Automatic,
            "automatic-corrected" => Self::AutomaticCorrected,
            "converted" => Self::Converted,
            "converted-corrected" => Self::ConvertedCorrected,
            "crowd-sourced" => Self::CrowdSourced,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for AnnotationLayerSourceMethod {
    fn from(s: &str) -> Self {
        match s {
            "manual-native" => Self::ManualNative,
            "manual-corrected" => Self::ManualCorrected,
            "automatic" => Self::Automatic,
            "automatic-corrected" => Self::AutomaticCorrected,
            "converted" => Self::Converted,
            "converted-corrected" => Self::ConvertedCorrected,
            "crowd-sourced" => Self::CrowdSourced,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for AnnotationLayerSourceMethod {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for AnnotationLayerSourceMethod {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// AnnotationLayerSubkind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum AnnotationLayerSubkind {
    Pos,
    Xpos,
    Ner,
    Lemma,
    Morph,
    Supersense,
    Sense,
    Chunk,
    Speaker,
    Gloss,
    Phonetic,
    Prosody,
    Tobi,
    LanguageId,
    EntityMention,
    SituationMention,
    Frame,
    Predicate,
    DiscourseUnit,
    SpeechAct,
    TemporalExpression,
    TemporalSignal,
    SpatialExpression,
    SpatialSignal,
    SpatialRelation,
    LocationMention,
    Sentiment,
    Emotion,
    Stance,
    InformationStructure,
    Error,
    Correction,
    CodeSwitch,
    Highlight,
    Comment,
    Bookmark,
    TemporalValue,
    TemporalVagueness,
    Dependency,
    EnhancedDependency,
    Constituency,
    Ccg,
    Coreference,
    Bridging,
    TemporalRelation,
    CausalRelation,
    DiscourseRelation,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl AnnotationLayerSubkind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Pos => "pos",
            Self::Xpos => "xpos",
            Self::Ner => "ner",
            Self::Lemma => "lemma",
            Self::Morph => "morph",
            Self::Supersense => "supersense",
            Self::Sense => "sense",
            Self::Chunk => "chunk",
            Self::Speaker => "speaker",
            Self::Gloss => "gloss",
            Self::Phonetic => "phonetic",
            Self::Prosody => "prosody",
            Self::Tobi => "tobi",
            Self::LanguageId => "language-id",
            Self::EntityMention => "entity-mention",
            Self::SituationMention => "situation-mention",
            Self::Frame => "frame",
            Self::Predicate => "predicate",
            Self::DiscourseUnit => "discourse-unit",
            Self::SpeechAct => "speech-act",
            Self::TemporalExpression => "temporal-expression",
            Self::TemporalSignal => "temporal-signal",
            Self::SpatialExpression => "spatial-expression",
            Self::SpatialSignal => "spatial-signal",
            Self::SpatialRelation => "spatial-relation",
            Self::LocationMention => "location-mention",
            Self::Sentiment => "sentiment",
            Self::Emotion => "emotion",
            Self::Stance => "stance",
            Self::InformationStructure => "information-structure",
            Self::Error => "error",
            Self::Correction => "correction",
            Self::CodeSwitch => "code-switch",
            Self::Highlight => "highlight",
            Self::Comment => "comment",
            Self::Bookmark => "bookmark",
            Self::TemporalValue => "temporal-value",
            Self::TemporalVagueness => "temporal-vagueness",
            Self::Dependency => "dependency",
            Self::EnhancedDependency => "enhanced-dependency",
            Self::Constituency => "constituency",
            Self::Ccg => "ccg",
            Self::Coreference => "coreference",
            Self::Bridging => "bridging",
            Self::TemporalRelation => "temporal-relation",
            Self::CausalRelation => "causal-relation",
            Self::DiscourseRelation => "discourse-relation",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for AnnotationLayerSubkind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "pos" => Self::Pos,
            "xpos" => Self::Xpos,
            "ner" => Self::Ner,
            "lemma" => Self::Lemma,
            "morph" => Self::Morph,
            "supersense" => Self::Supersense,
            "sense" => Self::Sense,
            "chunk" => Self::Chunk,
            "speaker" => Self::Speaker,
            "gloss" => Self::Gloss,
            "phonetic" => Self::Phonetic,
            "prosody" => Self::Prosody,
            "tobi" => Self::Tobi,
            "language-id" => Self::LanguageId,
            "entity-mention" => Self::EntityMention,
            "situation-mention" => Self::SituationMention,
            "frame" => Self::Frame,
            "predicate" => Self::Predicate,
            "discourse-unit" => Self::DiscourseUnit,
            "speech-act" => Self::SpeechAct,
            "temporal-expression" => Self::TemporalExpression,
            "temporal-signal" => Self::TemporalSignal,
            "spatial-expression" => Self::SpatialExpression,
            "spatial-signal" => Self::SpatialSignal,
            "spatial-relation" => Self::SpatialRelation,
            "location-mention" => Self::LocationMention,
            "sentiment" => Self::Sentiment,
            "emotion" => Self::Emotion,
            "stance" => Self::Stance,
            "information-structure" => Self::InformationStructure,
            "error" => Self::Error,
            "correction" => Self::Correction,
            "code-switch" => Self::CodeSwitch,
            "highlight" => Self::Highlight,
            "comment" => Self::Comment,
            "bookmark" => Self::Bookmark,
            "temporal-value" => Self::TemporalValue,
            "temporal-vagueness" => Self::TemporalVagueness,
            "dependency" => Self::Dependency,
            "enhanced-dependency" => Self::EnhancedDependency,
            "constituency" => Self::Constituency,
            "ccg" => Self::Ccg,
            "coreference" => Self::Coreference,
            "bridging" => Self::Bridging,
            "temporal-relation" => Self::TemporalRelation,
            "causal-relation" => Self::CausalRelation,
            "discourse-relation" => Self::DiscourseRelation,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for AnnotationLayerSubkind {
    fn from(s: &str) -> Self {
        match s {
            "pos" => Self::Pos,
            "xpos" => Self::Xpos,
            "ner" => Self::Ner,
            "lemma" => Self::Lemma,
            "morph" => Self::Morph,
            "supersense" => Self::Supersense,
            "sense" => Self::Sense,
            "chunk" => Self::Chunk,
            "speaker" => Self::Speaker,
            "gloss" => Self::Gloss,
            "phonetic" => Self::Phonetic,
            "prosody" => Self::Prosody,
            "tobi" => Self::Tobi,
            "language-id" => Self::LanguageId,
            "entity-mention" => Self::EntityMention,
            "situation-mention" => Self::SituationMention,
            "frame" => Self::Frame,
            "predicate" => Self::Predicate,
            "discourse-unit" => Self::DiscourseUnit,
            "speech-act" => Self::SpeechAct,
            "temporal-expression" => Self::TemporalExpression,
            "temporal-signal" => Self::TemporalSignal,
            "spatial-expression" => Self::SpatialExpression,
            "spatial-signal" => Self::SpatialSignal,
            "spatial-relation" => Self::SpatialRelation,
            "location-mention" => Self::LocationMention,
            "sentiment" => Self::Sentiment,
            "emotion" => Self::Emotion,
            "stance" => Self::Stance,
            "information-structure" => Self::InformationStructure,
            "error" => Self::Error,
            "correction" => Self::Correction,
            "code-switch" => Self::CodeSwitch,
            "highlight" => Self::Highlight,
            "comment" => Self::Comment,
            "bookmark" => Self::Bookmark,
            "temporal-value" => Self::TemporalValue,
            "temporal-vagueness" => Self::TemporalVagueness,
            "dependency" => Self::Dependency,
            "enhanced-dependency" => Self::EnhancedDependency,
            "constituency" => Self::Constituency,
            "ccg" => Self::Ccg,
            "coreference" => Self::Coreference,
            "bridging" => Self::Bridging,
            "temporal-relation" => Self::TemporalRelation,
            "causal-relation" => Self::CausalRelation,
            "discourse-relation" => Self::DiscourseRelation,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for AnnotationLayerSubkind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for AnnotationLayerSubkind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
