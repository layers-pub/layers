// @generated by idiolect-codegen. do not edit.
// source: pub.layers.annotation.clusterSet

//! Generated from `pub.layers.annotation.clusterSet`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// Groups annotations into equivalence classes. Used for coreference resolution (entity clusters, situation clusters), bridging anaphora grouping, and any annotation clustering task.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ClusterSet {
    pub clusters: Vec<crate::generated::r#pub::layers::annotation::defs::Cluster>,
    /// Corpus these clusters span (for cross-document clustering).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub corpus_ref: Option<idiolect_records::AtUri>,
    pub created_at: idiolect_records::Datetime,
    /// Primary expression context. Optional for cross-document clustering.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expression: Option<idiolect_records::AtUri>,
    /// For cross-document clustering: all expressions these clusters span.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expression_refs: Option<Vec<idiolect_records::AtUri>>,
    /// Clustering kind slug (fallback when kindUri unavailable).
    pub kind: ClusterSetKind,
    /// AT-URI of the clustering kind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind_uri: Option<idiolect_records::AtUri>,
    /// The annotation layer whose annotations these clusters group.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub layer_ref: Option<idiolect_records::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<crate::generated::r#pub::layers::defs::AnnotationMetadata>,
}

impl crate::Record for ClusterSet {
    const NSID: &'static str = "pub.layers.annotation.clusterSet";
}

/// ClusterSetKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ClusterSetKind {
    Coreference,
    SituationCoreference,
    Bridging,
    SameAs,
    Clustering,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl ClusterSetKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Coreference => "coreference",
            Self::SituationCoreference => "situation-coreference",
            Self::Bridging => "bridging",
            Self::SameAs => "same-as",
            Self::Clustering => "clustering",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ClusterSetKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "coreference" => Self::Coreference,
            "situation-coreference" => Self::SituationCoreference,
            "bridging" => Self::Bridging,
            "same-as" => Self::SameAs,
            "clustering" => Self::Clustering,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for ClusterSetKind {
    fn from(s: &str) -> Self {
        match s {
            "coreference" => Self::Coreference,
            "situation-coreference" => Self::SituationCoreference,
            "bridging" => Self::Bridging,
            "same-as" => Self::SameAs,
            "clustering" => Self::Clustering,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for ClusterSetKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ClusterSetKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
