// @generated by idiolect-codegen. do not edit.
// source: pub.layers.expression.expression

//! An Expression is the primary document model in Layers. It represents any linguistic unit (a document, transcript, recording, paragraph, sentence, word, morpheme) with recursive nesting via parent references. Inspired by Concrete's Communication, generalized to support recursive sub-expression structure.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// An expression record representing a linguistic data source or unit at any granularity, from full documents down to individual morphemes.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Expression {
    /// How this expression attaches to its parent (character span, temporal span, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub anchor: Option<crate::generated::r#pub::layers::defs::Anchor>,
    pub created_at: idiolect_records::Datetime,
    /// Reference to an eprint record that this expression is associated with.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub eprint_ref: Option<idiolect_records::AtUri>,
    /// Arbitrary document-level features and metadata.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// A corpus-level unique identifier (headline, URL, document ID, etc.).
    pub id: String,
    /// Expression kind slug (fallback when kindUri unavailable).
    pub kind: ExpressionKind,
    /// AT-URI of the expression kind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind_uri: Option<idiolect_records::AtUri>,
    /// References to knowledge base entries relevant to this expression.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// BCP-47 language tags this record covers. Empty when language is unspecified or unknown.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub languages: Option<Vec<String>>,
    /// Optional inline media blob.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub media_blob: Option<serde_json::Value>,
    /// Reference to an associated media record (audio, video, image).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub media_ref: Option<idiolect_records::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<crate::generated::r#pub::layers::defs::AnnotationMetadata>,
    /// Reference to the parent Expression this one is nested within. Absent for top-level expressions (documents, recordings, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_ref: Option<idiolect_records::AtUri>,
    /// AT-URI of an external ATProto record this expression is derived from or annotates (e.g., a standard.site Leaflet post, a com.whtwnd blog entry, an app.bsky.feed.post, an at.margin.bookmark).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_ref: Option<idiolect_records::AtUri>,
    /// URL of the external web resource this expression was derived from or annotates. The appview indexes this field to discover co-located annotations from other ATProto apps.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_url: Option<idiolect_records::Uri>,
    /// The full raw text of the expression. All byte-offset spans reference this string.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub text: Option<String>,
}

impl crate::Record for Expression {
    const NSID: &'static str = "pub.layers.expression.expression";
}

/// ExpressionKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ExpressionKind {
    Document,
    Transcript,
    Dialogue,
    SocialMedia,
    Email,
    Article,
    Recording,
    Video,
    Multimodal,
    Code,
    Section,
    Paragraph,
    Chapter,
    Turn,
    Utterance,
    Heading,
    List,
    Sentence,
    Clause,
    Phrase,
    Word,
    Morpheme,
    Character,
    Other,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Extended(String),
}
impl ExpressionKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Document => "document",
            Self::Transcript => "transcript",
            Self::Dialogue => "dialogue",
            Self::SocialMedia => "social-media",
            Self::Email => "email",
            Self::Article => "article",
            Self::Recording => "recording",
            Self::Video => "video",
            Self::Multimodal => "multimodal",
            Self::Code => "code",
            Self::Section => "section",
            Self::Paragraph => "paragraph",
            Self::Chapter => "chapter",
            Self::Turn => "turn",
            Self::Utterance => "utterance",
            Self::Heading => "heading",
            Self::List => "list",
            Self::Sentence => "sentence",
            Self::Clause => "clause",
            Self::Phrase => "phrase",
            Self::Word => "word",
            Self::Morpheme => "morpheme",
            Self::Character => "character",
            Self::Other => "other",
            Self::Extended(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ExpressionKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "document" => Self::Document,
            "transcript" => Self::Transcript,
            "dialogue" => Self::Dialogue,
            "social-media" => Self::SocialMedia,
            "email" => Self::Email,
            "article" => Self::Article,
            "recording" => Self::Recording,
            "video" => Self::Video,
            "multimodal" => Self::Multimodal,
            "code" => Self::Code,
            "section" => Self::Section,
            "paragraph" => Self::Paragraph,
            "chapter" => Self::Chapter,
            "turn" => Self::Turn,
            "utterance" => Self::Utterance,
            "heading" => Self::Heading,
            "list" => Self::List,
            "sentence" => Self::Sentence,
            "clause" => Self::Clause,
            "phrase" => Self::Phrase,
            "word" => Self::Word,
            "morpheme" => Self::Morpheme,
            "character" => Self::Character,
            "other" => Self::Other,
            _ => Self::Extended(s),
        }
    }
}
impl From<&str> for ExpressionKind {
    fn from(s: &str) -> Self {
        match s {
            "document" => Self::Document,
            "transcript" => Self::Transcript,
            "dialogue" => Self::Dialogue,
            "social-media" => Self::SocialMedia,
            "email" => Self::Email,
            "article" => Self::Article,
            "recording" => Self::Recording,
            "video" => Self::Video,
            "multimodal" => Self::Multimodal,
            "code" => Self::Code,
            "section" => Self::Section,
            "paragraph" => Self::Paragraph,
            "chapter" => Self::Chapter,
            "turn" => Self::Turn,
            "utterance" => Self::Utterance,
            "heading" => Self::Heading,
            "list" => Self::List,
            "sentence" => Self::Sentence,
            "clause" => Self::Clause,
            "phrase" => Self::Phrase,
            "word" => Self::Word,
            "morpheme" => Self::Morpheme,
            "character" => Self::Character,
            "other" => Self::Other,
            _ => Self::Extended(s.to_owned()),
        }
    }
}
impl serde::Serialize for ExpressionKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ExpressionKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
