// @generated by idiolect-codegen. do not edit.
// source: pub.layers.eprint.eprint

//! Generated from `pub.layers.eprint.eprint`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A link between a Layers data record and an eprint.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Eprint {
    /// References to specific annotation records linked to this eprint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub annotation_refs: Option<Vec<idiolect_records::AtUri>>,
    /// Full citation string.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub citation: Option<String>,
    /// Reference to a corpus record.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub corpus_ref: Option<idiolect_records::AtUri>,
    pub created_at: idiolect_records::Datetime,
    /// Description of the relationship.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// The eprint identifier (DOI, arXiv ID, ACL Anthology ID, etc.).
    pub eprint_identifier: String,
    /// Identifier type slug (fallback when eprintIdentifierTypeUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub eprint_identifier_type: Option<EprintEprintIdentifierType>,
    /// AT-URI of the identifier type definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub eprint_identifier_type_uri: Option<idiolect_records::AtUri>,
    /// Full URI of the eprint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub eprint_uri: Option<idiolect_records::Uri>,
    /// References to Layers expressions linked to this eprint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expression_refs: Option<Vec<idiolect_records::AtUri>>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Knowledge graph references (e.g., Wikidata for the venue, DBLP, Semantic Scholar corpus ID).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// Link type slug (fallback when linkTypeUri unavailable).
    pub link_type: EprintLinkType,
    /// AT-URI of the link type definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub link_type_uri: Option<idiolect_records::AtUri>,
    /// AT-URI of the eprint record on its publication platform (e.g., chive.pub, any ATProto-native publication service).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub platform_eprint_ref: Option<idiolect_records::AtUri>,
}

impl crate::Record for Eprint {
    const NSID: &'static str = "pub.layers.eprint.eprint";
}

/// EprintEprintIdentifierType. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum EprintEprintIdentifierType {
    Doi,
    Arxiv,
    AclAnthology,
    SemanticScholar,
    Pubmed,
    Isbn,
    Url,
    AtUri,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl EprintEprintIdentifierType {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Doi => "doi",
            Self::Arxiv => "arxiv",
            Self::AclAnthology => "acl-anthology",
            Self::SemanticScholar => "semantic-scholar",
            Self::Pubmed => "pubmed",
            Self::Isbn => "isbn",
            Self::Url => "url",
            Self::AtUri => "at-uri",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for EprintEprintIdentifierType {
    fn from(s: String) -> Self {
        match s.as_str() {
            "doi" => Self::Doi,
            "arxiv" => Self::Arxiv,
            "acl-anthology" => Self::AclAnthology,
            "semantic-scholar" => Self::SemanticScholar,
            "pubmed" => Self::Pubmed,
            "isbn" => Self::Isbn,
            "url" => Self::Url,
            "at-uri" => Self::AtUri,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for EprintEprintIdentifierType {
    fn from(s: &str) -> Self {
        match s {
            "doi" => Self::Doi,
            "arxiv" => Self::Arxiv,
            "acl-anthology" => Self::AclAnthology,
            "semantic-scholar" => Self::SemanticScholar,
            "pubmed" => Self::Pubmed,
            "isbn" => Self::Isbn,
            "url" => Self::Url,
            "at-uri" => Self::AtUri,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for EprintEprintIdentifierType {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for EprintEprintIdentifierType {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// EprintLinkType. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum EprintLinkType {
    ProducedBy,
    DescribedIn,
    EvaluatedIn,
    ReplicatedFrom,
    Extends,
    Supplements,
    CitedIn,
    Annotates,
    TrainingDataFor,
    TestDataFor,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl EprintLinkType {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::ProducedBy => "produced-by",
            Self::DescribedIn => "described-in",
            Self::EvaluatedIn => "evaluated-in",
            Self::ReplicatedFrom => "replicated-from",
            Self::Extends => "extends",
            Self::Supplements => "supplements",
            Self::CitedIn => "cited-in",
            Self::Annotates => "annotates",
            Self::TrainingDataFor => "training-data-for",
            Self::TestDataFor => "test-data-for",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for EprintLinkType {
    fn from(s: String) -> Self {
        match s.as_str() {
            "produced-by" => Self::ProducedBy,
            "described-in" => Self::DescribedIn,
            "evaluated-in" => Self::EvaluatedIn,
            "replicated-from" => Self::ReplicatedFrom,
            "extends" => Self::Extends,
            "supplements" => Self::Supplements,
            "cited-in" => Self::CitedIn,
            "annotates" => Self::Annotates,
            "training-data-for" => Self::TrainingDataFor,
            "test-data-for" => Self::TestDataFor,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for EprintLinkType {
    fn from(s: &str) -> Self {
        match s {
            "produced-by" => Self::ProducedBy,
            "described-in" => Self::DescribedIn,
            "evaluated-in" => Self::EvaluatedIn,
            "replicated-from" => Self::ReplicatedFrom,
            "extends" => Self::Extends,
            "supplements" => Self::Supplements,
            "cited-in" => Self::CitedIn,
            "annotates" => Self::Annotates,
            "training-data-for" => Self::TrainingDataFor,
            "test-data-for" => Self::TestDataFor,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for EprintLinkType {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for EprintLinkType {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
