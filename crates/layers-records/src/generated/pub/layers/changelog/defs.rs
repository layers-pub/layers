// @generated by idiolect-codegen. do not edit.
// source: pub.layers.changelog.defs

//! Generated from `pub.layers.changelog.defs`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// An individual change entry. The targets field uses objectRef for machine-readable sub-record targeting, allowing a change item to point at specific objects within the subject record.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ChangeItem {
    /// Type of change.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub change_type: Option<ChangeItemChangeType>,
    /// Description of the change.
    pub description: String,
    /// Path to the changed field within the target (e.g., 'annotations/3/label', 'formalism', 'annotationDesign/guidelinesRef').
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub field_path: Option<String>,
    /// New value as a display string.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_value: Option<String>,
    /// Previous value as a display string.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub previous_value: Option<String>,
    /// Specific objects that changed. Uses recordRef + objectId for sub-record targeting (e.g., a specific annotation within an annotation layer).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub targets: Option<Vec<crate::generated::r#pub::layers::defs::ObjectRef>>,
}

/// A group of changes under a single category.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ChangeSection {
    /// Category of changes.
    pub category: ChangeSectionCategory,
    /// Individual change items in this section.
    pub items: Vec<ChangeItem>,
}

/// A semantic version following the major.minor.patch convention.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SemanticVersion {
    /// Major version number.
    pub major: i64,
    /// Minor version number.
    pub minor: i64,
    /// Patch version number.
    pub patch: i64,
}

/// ChangeItemChangeType. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ChangeItemChangeType {
    Added,
    Changed,
    Removed,
    Fixed,
    Deprecated,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl ChangeItemChangeType {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Added => "added",
            Self::Changed => "changed",
            Self::Removed => "removed",
            Self::Fixed => "fixed",
            Self::Deprecated => "deprecated",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ChangeItemChangeType {
    fn from(s: String) -> Self {
        match s.as_str() {
            "added" => Self::Added,
            "changed" => Self::Changed,
            "removed" => Self::Removed,
            "fixed" => Self::Fixed,
            "deprecated" => Self::Deprecated,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for ChangeItemChangeType {
    fn from(s: &str) -> Self {
        match s {
            "added" => Self::Added,
            "changed" => Self::Changed,
            "removed" => Self::Removed,
            "fixed" => Self::Fixed,
            "deprecated" => Self::Deprecated,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for ChangeItemChangeType {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ChangeItemChangeType {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// ChangeSectionCategory. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ChangeSectionCategory {
    Annotations,
    Segmentation,
    Text,
    Ontology,
    Corpus,
    Alignment,
    Graph,
    Experiment,
    Resource,
    Media,
    Provenance,
    References,
    Corrections,
    Other,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Extended(String),
}
impl ChangeSectionCategory {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Annotations => "annotations",
            Self::Segmentation => "segmentation",
            Self::Text => "text",
            Self::Ontology => "ontology",
            Self::Corpus => "corpus",
            Self::Alignment => "alignment",
            Self::Graph => "graph",
            Self::Experiment => "experiment",
            Self::Resource => "resource",
            Self::Media => "media",
            Self::Provenance => "provenance",
            Self::References => "references",
            Self::Corrections => "corrections",
            Self::Other => "other",
            Self::Extended(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for ChangeSectionCategory {
    fn from(s: String) -> Self {
        match s.as_str() {
            "annotations" => Self::Annotations,
            "segmentation" => Self::Segmentation,
            "text" => Self::Text,
            "ontology" => Self::Ontology,
            "corpus" => Self::Corpus,
            "alignment" => Self::Alignment,
            "graph" => Self::Graph,
            "experiment" => Self::Experiment,
            "resource" => Self::Resource,
            "media" => Self::Media,
            "provenance" => Self::Provenance,
            "references" => Self::References,
            "corrections" => Self::Corrections,
            "other" => Self::Other,
            _ => Self::Extended(s),
        }
    }
}
impl From<&str> for ChangeSectionCategory {
    fn from(s: &str) -> Self {
        match s {
            "annotations" => Self::Annotations,
            "segmentation" => Self::Segmentation,
            "text" => Self::Text,
            "ontology" => Self::Ontology,
            "corpus" => Self::Corpus,
            "alignment" => Self::Alignment,
            "graph" => Self::Graph,
            "experiment" => Self::Experiment,
            "resource" => Self::Resource,
            "media" => Self::Media,
            "provenance" => Self::Provenance,
            "references" => Self::References,
            "corrections" => Self::Corrections,
            "other" => Self::Other,
            _ => Self::Extended(s.to_owned()),
        }
    }
}
impl serde::Serialize for ChangeSectionCategory {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for ChangeSectionCategory {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
