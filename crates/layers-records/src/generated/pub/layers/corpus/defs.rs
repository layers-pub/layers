// @generated by idiolect-codegen. do not edit.
// source: pub.layers.corpus.defs

//! Generated from `pub.layers.corpus.defs`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// How disagreements between annotators are resolved into a final annotation.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct AdjudicationSpec {
    /// Agreement level (0-1000) above which adjudication is skipped.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub agreement_threshold: Option<i64>,
    /// Whether a separate adjudicator (not one of the annotators) resolves disagreements.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dedicated_adjudicator: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Adjudication method (fallback when methodUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub method: Option<AdjudicationSpecMethod>,
    /// AT-URI of the adjudication method definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub method_uri: Option<idiolect_records::AtUri>,
}

/// Annotation project design parameters: annotator assignment, adjudication, and quality criteria.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct AnnotationDesign {
    /// How disagreements are resolved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub adjudication: Option<AdjudicationSpec>,
    /// Number of annotation passes in the project workflow.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub annotation_rounds: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// AT-URI of the annotation guidelines document (e.g., a pub.layers.persona or external resource).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub guidelines_ref: Option<idiolect_records::AtUri>,
    /// Version identifier for the annotation guidelines.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub guidelines_version: Option<String>,
    /// Acceptance criteria for annotation quality.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub quality_criteria: Option<Vec<QualityCriterion>>,
    /// How annotators are assigned to items.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub redundancy: Option<RedundancySpec>,
}

/// An acceptance criterion for annotation quality.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct QualityCriterion {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Agreement or quality metric (fallback when metricUri unavailable).
    pub metric: QualityCriterionMetric,
    /// AT-URI of the metric definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metric_uri: Option<idiolect_records::AtUri>,
    /// Evaluation scope (fallback when scopeUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scope: Option<QualityCriterionScope>,
    /// AT-URI of the evaluation scope definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scope_uri: Option<idiolect_records::AtUri>,
    /// Minimum acceptable metric value (0-1000).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub threshold: Option<i64>,
}

/// How many annotators work on each item and how they are assigned.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct RedundancySpec {
    /// Total number of annotators in the project.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub annotator_pool: Option<i64>,
    /// How annotators are assigned to items (fallback when assignmentStrategyUri unavailable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub assignment_strategy: Option<RedundancySpecAssignmentStrategy>,
    /// AT-URI of the assignment strategy definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub assignment_strategy_uri: Option<idiolect_records::AtUri>,
    /// Number of independent annotators per item.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub count: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
}

/// AdjudicationSpecMethod. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum AdjudicationSpecMethod {
    Expert,
    MajorityVote,
    Unanimous,
    Discussion,
    DawidSkene,
    AutomaticMerge,
    Intersection,
    Union,
    None,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl AdjudicationSpecMethod {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Expert => "expert",
            Self::MajorityVote => "majority-vote",
            Self::Unanimous => "unanimous",
            Self::Discussion => "discussion",
            Self::DawidSkene => "dawid-skene",
            Self::AutomaticMerge => "automatic-merge",
            Self::Intersection => "intersection",
            Self::Union => "union",
            Self::None => "none",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for AdjudicationSpecMethod {
    fn from(s: String) -> Self {
        match s.as_str() {
            "expert" => Self::Expert,
            "majority-vote" => Self::MajorityVote,
            "unanimous" => Self::Unanimous,
            "discussion" => Self::Discussion,
            "dawid-skene" => Self::DawidSkene,
            "automatic-merge" => Self::AutomaticMerge,
            "intersection" => Self::Intersection,
            "union" => Self::Union,
            "none" => Self::None,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for AdjudicationSpecMethod {
    fn from(s: &str) -> Self {
        match s {
            "expert" => Self::Expert,
            "majority-vote" => Self::MajorityVote,
            "unanimous" => Self::Unanimous,
            "discussion" => Self::Discussion,
            "dawid-skene" => Self::DawidSkene,
            "automatic-merge" => Self::AutomaticMerge,
            "intersection" => Self::Intersection,
            "union" => Self::Union,
            "none" => Self::None,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for AdjudicationSpecMethod {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for AdjudicationSpecMethod {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// QualityCriterionMetric. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum QualityCriterionMetric {
    CohensKappa,
    FleissKappa,
    KrippendorffAlpha,
    PercentAgreement,
    F1,
    Smatch,
    Uas,
    Las,
    Correlation,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl QualityCriterionMetric {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::CohensKappa => "cohens-kappa",
            Self::FleissKappa => "fleiss-kappa",
            Self::KrippendorffAlpha => "krippendorff-alpha",
            Self::PercentAgreement => "percent-agreement",
            Self::F1 => "f1",
            Self::Smatch => "smatch",
            Self::Uas => "uas",
            Self::Las => "las",
            Self::Correlation => "correlation",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for QualityCriterionMetric {
    fn from(s: String) -> Self {
        match s.as_str() {
            "cohens-kappa" => Self::CohensKappa,
            "fleiss-kappa" => Self::FleissKappa,
            "krippendorff-alpha" => Self::KrippendorffAlpha,
            "percent-agreement" => Self::PercentAgreement,
            "f1" => Self::F1,
            "smatch" => Self::Smatch,
            "uas" => Self::Uas,
            "las" => Self::Las,
            "correlation" => Self::Correlation,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for QualityCriterionMetric {
    fn from(s: &str) -> Self {
        match s {
            "cohens-kappa" => Self::CohensKappa,
            "fleiss-kappa" => Self::FleissKappa,
            "krippendorff-alpha" => Self::KrippendorffAlpha,
            "percent-agreement" => Self::PercentAgreement,
            "f1" => Self::F1,
            "smatch" => Self::Smatch,
            "uas" => Self::Uas,
            "las" => Self::Las,
            "correlation" => Self::Correlation,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for QualityCriterionMetric {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for QualityCriterionMetric {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// QualityCriterionScope. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum QualityCriterionScope {
    Item,
    Layer,
    Document,
    Corpus,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl QualityCriterionScope {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Item => "item",
            Self::Layer => "layer",
            Self::Document => "document",
            Self::Corpus => "corpus",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for QualityCriterionScope {
    fn from(s: String) -> Self {
        match s.as_str() {
            "item" => Self::Item,
            "layer" => Self::Layer,
            "document" => Self::Document,
            "corpus" => Self::Corpus,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for QualityCriterionScope {
    fn from(s: &str) -> Self {
        match s {
            "item" => Self::Item,
            "layer" => Self::Layer,
            "document" => Self::Document,
            "corpus" => Self::Corpus,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for QualityCriterionScope {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for QualityCriterionScope {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// RedundancySpecAssignmentStrategy. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum RedundancySpecAssignmentStrategy {
    Random,
    RoundRobin,
    Stratified,
    ExpertiseBased,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl RedundancySpecAssignmentStrategy {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Random => "random",
            Self::RoundRobin => "round-robin",
            Self::Stratified => "stratified",
            Self::ExpertiseBased => "expertise-based",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for RedundancySpecAssignmentStrategy {
    fn from(s: String) -> Self {
        match s.as_str() {
            "random" => Self::Random,
            "round-robin" => Self::RoundRobin,
            "stratified" => Self::Stratified,
            "expertise-based" => Self::ExpertiseBased,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for RedundancySpecAssignmentStrategy {
    fn from(s: &str) -> Self {
        match s {
            "random" => Self::Random,
            "round-robin" => Self::RoundRobin,
            "stratified" => Self::Stratified,
            "expertise-based" => Self::ExpertiseBased,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for RedundancySpecAssignmentStrategy {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for RedundancySpecAssignmentStrategy {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
