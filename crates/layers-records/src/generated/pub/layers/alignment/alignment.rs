// @generated by idiolect-codegen. do not edit.
// source: pub.layers.alignment.alignment

//! Alignment records for parallel structure correspondence. Handles interlinear glossing (Leipzig glossing rules), parallel text alignment (translation), cross-tokenization mapping (word-to-morpheme), audio-text forced alignment, and any many-to-many correspondence between annotation elements or sequences.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// An alignment between two parallel sequences. The sequences can be tokenizations, annotation layers, expressions (for parallel text), or tiers. Links establish many-to-many correspondence between elements indexed by position.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Alignment {
    pub created_at: idiolect_records::Datetime,
    /// Primary expression context (for within-document alignments).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expression: Option<idiolect_records::AtUri>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Alignment kind slug (fallback). The type of alignment.
    pub kind: AlignmentKind,
    /// AT-URI of the alignment kind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind_uri: Option<idiolect_records::AtUri>,
    /// Knowledge graph references for this alignment (e.g., alignment model, parallel corpus source).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// The alignment links.
    pub links: Vec<crate::generated::r#pub::layers::defs::AlignmentLink>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<crate::generated::r#pub::layers::defs::AnnotationMetadata>,
    /// Reference to the source sequence. Use localId for within-segmentation tokenization UUID, recordRef for cross-record AT-URI (annotation layer, expression, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<crate::generated::r#pub::layers::defs::ObjectRef>,
    /// BCP-47 language tag for the source (for parallel text alignment).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_lang: Option<String>,
    /// Alignment subkind slug (fallback). More specific alignment type within the kind.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subkind: Option<AlignmentSubkind>,
    /// AT-URI of the alignment subkind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subkind_uri: Option<idiolect_records::AtUri>,
    /// Reference to the target sequence. Use localId for within-segmentation tokenization UUID, recordRef for cross-record AT-URI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target: Option<crate::generated::r#pub::layers::defs::ObjectRef>,
    /// BCP-47 language tag for the target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target_lang: Option<String>,
}

impl crate::Record for Alignment {
    const NSID: &'static str = "pub.layers.alignment.alignment";
}

/// AlignmentKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum AlignmentKind {
    TokenizationToTokenization,
    Interlinear,
    ParallelText,
    AudioToText,
    LayerToLayer,
    ErrorToCorrection,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl AlignmentKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::TokenizationToTokenization => "tokenization-to-tokenization",
            Self::Interlinear => "interlinear",
            Self::ParallelText => "parallel-text",
            Self::AudioToText => "audio-to-text",
            Self::LayerToLayer => "layer-to-layer",
            Self::ErrorToCorrection => "error-to-correction",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for AlignmentKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "tokenization-to-tokenization" => Self::TokenizationToTokenization,
            "interlinear" => Self::Interlinear,
            "parallel-text" => Self::ParallelText,
            "audio-to-text" => Self::AudioToText,
            "layer-to-layer" => Self::LayerToLayer,
            "error-to-correction" => Self::ErrorToCorrection,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for AlignmentKind {
    fn from(s: &str) -> Self {
        match s {
            "tokenization-to-tokenization" => Self::TokenizationToTokenization,
            "interlinear" => Self::Interlinear,
            "parallel-text" => Self::ParallelText,
            "audio-to-text" => Self::AudioToText,
            "layer-to-layer" => Self::LayerToLayer,
            "error-to-correction" => Self::ErrorToCorrection,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for AlignmentKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for AlignmentKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}

/// AlignmentSubkind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum AlignmentSubkind {
    WordToMorpheme,
    WordToWord,
    SentenceToSentence,
    PhraseToPhrase,
    MorphemeToGloss,
    ForcedAlignment,
    ManualAlignment,
    Custom,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl AlignmentSubkind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::WordToMorpheme => "word-to-morpheme",
            Self::WordToWord => "word-to-word",
            Self::SentenceToSentence => "sentence-to-sentence",
            Self::PhraseToPhrase => "phrase-to-phrase",
            Self::MorphemeToGloss => "morpheme-to-gloss",
            Self::ForcedAlignment => "forced-alignment",
            Self::ManualAlignment => "manual-alignment",
            Self::Custom => "custom",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for AlignmentSubkind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "word-to-morpheme" => Self::WordToMorpheme,
            "word-to-word" => Self::WordToWord,
            "sentence-to-sentence" => Self::SentenceToSentence,
            "phrase-to-phrase" => Self::PhraseToPhrase,
            "morpheme-to-gloss" => Self::MorphemeToGloss,
            "forced-alignment" => Self::ForcedAlignment,
            "manual-alignment" => Self::ManualAlignment,
            "custom" => Self::Custom,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for AlignmentSubkind {
    fn from(s: &str) -> Self {
        match s {
            "word-to-morpheme" => Self::WordToMorpheme,
            "word-to-word" => Self::WordToWord,
            "sentence-to-sentence" => Self::SentenceToSentence,
            "phrase-to-phrase" => Self::PhraseToPhrase,
            "morpheme-to-gloss" => Self::MorphemeToGloss,
            "forced-alignment" => Self::ForcedAlignment,
            "manual-alignment" => Self::ManualAlignment,
            "custom" => Self::Custom,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for AlignmentSubkind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for AlignmentSubkind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
