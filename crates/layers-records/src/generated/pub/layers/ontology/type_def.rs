// @generated by idiolect-codegen. do not edit.
// source: pub.layers.ontology.typeDef

//! Generated from `pub.layers.ontology.typeDef`.

#![allow(
    missing_docs,
    clippy::doc_markdown,
    clippy::struct_excessive_bools,
    clippy::derive_partial_eq_without_eq,
    clippy::large_enum_variant
)]
use serde::{Deserialize, Serialize};

/// A type definition within an ontology. Covers entity types, situation types, role types, and relation types in a single unified model.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TypeDef {
    /// For frame/situation types: the roles that can be filled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub allowed_roles: Option<Vec<crate::generated::r#pub::layers::ontology::defs::RoleSlot>>,
    /// For attribute types: enumerated allowed values.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub allowed_values: Option<Vec<String>>,
    pub created_at: idiolect_records::Datetime,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub features: Option<crate::generated::r#pub::layers::defs::FeatureMap>,
    /// Rich text definition/gloss of this type. May include references to other types and Wikidata entities, following FOVEA conventions.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gloss: Option<String>,
    /// Knowledge graph groundings (Wikidata, chive.pub, FrameNet, etc.).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub knowledge_refs: Option<Vec<crate::generated::r#pub::layers::defs::KnowledgeRef>>,
    /// The type name/label.
    pub name: String,
    /// The ontology this type belongs to.
    pub ontology_ref: idiolect_records::AtUri,
    /// Reference to a parent type (for type hierarchies/inheritance).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_type_ref: Option<idiolect_records::AtUri>,
    /// Type kind slug (fallback when typeKindUri unavailable).
    pub type_kind: TypeDefTypeKind,
    /// AT-URI of the type kind definition node. Community-expandable via knowledge graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_kind_uri: Option<idiolect_records::AtUri>,
}

impl crate::Record for TypeDef {
    const NSID: &'static str = "pub.layers.ontology.typeDef";
}

/// TypeDefTypeKind. Open-enum slug; known values are kebab-cased; community-extended values pass through as `Other(String)`.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum TypeDefTypeKind {
    EntityType,
    SituationType,
    RoleType,
    RelationType,
    AttributeType,
    /// Community-extended slug not present in the lexicon's
    /// `knownValues`. Resolves through the sibling
    /// `*Vocab` field on the containing record.
    Other(String),
}
impl TypeDefTypeKind {
    /// Wire-form slug for this value. Known variants render
    /// kebab-case; the fallback variant passes through verbatim.
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::EntityType => "entity-type",
            Self::SituationType => "situation-type",
            Self::RoleType => "role-type",
            Self::RelationType => "relation-type",
            Self::AttributeType => "attribute-type",
            Self::Other(s) => s.as_str(),
        }
    }
    /// Whether this slug is subsumed by `ancestor` under the
    /// `subsumed_by` relation in the supplied vocab. Reflexive:
    /// every slug is subsumed by itself.
    #[must_use]
    pub fn is_subsumed_by(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        ancestor: &str,
    ) -> bool {
        vocab.is_subsumed_by(self.as_str(), ancestor)
    }
    /// Whether this slug satisfies a requirement of `target`
    /// under the named `relation` in the supplied vocab.
    /// Generalises `is_subsumed_by` to any directed relation
    /// (e.g. `stronger_than`, `provides_at_least`,
    /// `equivalent_to`). Reflexive: a slug satisfies itself.
    #[must_use]
    pub fn satisfies(
        &self,
        vocab: &idiolect_records::vocab::VocabGraph,
        relation: &str,
        target: &str,
    ) -> bool {
        if self.as_str() == target {
            return true;
        }
        vocab
            .walk_relation(self.as_str(), relation, false)
            .iter()
            .any(|n| n == target)
    }
    /// Translate this slug across vocabularies via
    /// `equivalent_to` edges. Returns the translated slug as
    /// a target enum value when a translation exists, `None`
    /// when no path is found (callers fall back to passing
    /// the slug through verbatim, which is wire-compatible).
    #[must_use]
    pub fn translate_to<T: From<String>>(
        &self,
        src_vocab_uri: &str,
        tgt_vocab_uri: &str,
        registry: &idiolect_records::vocab::VocabRegistry,
    ) -> Option<T> {
        registry
            .translate(src_vocab_uri, tgt_vocab_uri, self.as_str())
            .map(T::from)
    }
}
impl From<String> for TypeDefTypeKind {
    fn from(s: String) -> Self {
        match s.as_str() {
            "entity-type" => Self::EntityType,
            "situation-type" => Self::SituationType,
            "role-type" => Self::RoleType,
            "relation-type" => Self::RelationType,
            "attribute-type" => Self::AttributeType,
            _ => Self::Other(s),
        }
    }
}
impl From<&str> for TypeDefTypeKind {
    fn from(s: &str) -> Self {
        match s {
            "entity-type" => Self::EntityType,
            "situation-type" => Self::SituationType,
            "role-type" => Self::RoleType,
            "relation-type" => Self::RelationType,
            "attribute-type" => Self::AttributeType,
            _ => Self::Other(s.to_owned()),
        }
    }
}
impl serde::Serialize for TypeDefTypeKind {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}
impl<'de> serde::Deserialize<'de> for TypeDefTypeKind {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
